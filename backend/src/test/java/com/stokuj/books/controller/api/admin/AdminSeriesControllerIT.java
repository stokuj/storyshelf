package com.stokuj.books.controller.api.admin;

public class AdminSeriesControllerIT {
    // TODO: Security: Authorization test that blocks anonymous guest from modifying series.
    // TODO: Validation: Ensure sending SeriesRequest without 'status' (Enum) returns proper validation error (400 Bad Request instead of 500).
}
