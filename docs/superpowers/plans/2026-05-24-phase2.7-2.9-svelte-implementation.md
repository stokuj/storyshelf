# Plan: Phase 2.7–2.9 — Svelte Frontend Implementation

> Spec: [`specs/2026-05-24-phase2.7-2.9-svelte-implementation.md`](../specs/2026-05-24-phase2.7-2.9-svelte-implementation.md)
> Data: 2026-05-24
> Strategia: subagenci równolegli per faza, lint po każdej fazie, TypeScript strict przez cały czas.

---

## Zasady wykonania

- **Subagenci:** każda faza (2.7 / 2.8 / 2.9) to osobny subagent uruchamiany po zakończeniu poprzedniej
- **Dokumentacja live:** przed implementacją każdego bloku — `context7` MCP dla SvelteKit 2, shadcn-svelte, superforms, @xyflow/svelte
- **Svelte 5 runes only:** `$state`, `$derived`, `$props`, `$effect` — żadnego `export let`, `$:`, legacy stores
- **TypeScript strict:** `$props()` z destructured TS interface, `$types` z `./$types`, brak `any`
- **Lint gate:** po każdej fazie `pnpm check && pnpm lint` musi przejść z 0 errors/warnings zanim zacznie się następna faza
- **Branch:** `phase/2.7-svelte-foundation` → PR → merge → `phase/2.8-svelte-books` → itd.
- **Working dir:** `svelte-frontend/`

---

## Faza 2.7 — Foundation

**Branch:** `phase/2.7-svelte-foundation`
**Subagent:** uruchom po checkout brancha

### Krok 2.7.1 — shadcn-svelte + deps

```bash
cd svelte-frontend
pnpm dlx shadcn-svelte@latest init
pnpm dlx shadcn-svelte@latest add button card dialog tabs avatar input label switch radio-group dropdown-menu sheet sonner tooltip badge separator skeleton command popover form
pnpm add lucide-svelte mode-watcher @xyflow/svelte sveltekit-superforms zod
```

> Sprawdź w context7 (`/llmstxt/shadcn-svelte_llms_txt`) aktualną składnię `init` i listę komponentów przed uruchomieniem.

### Krok 2.7.2 — Tailwind theme + fonts

**`src/app.html`** — dodaj `<link>` dla Newsreader (serif, display) i Public Sans (sans, body) z Google Fonts.

**`src/app.css`** — pełny `@theme` block:
```css
@theme {
  --color-paper: <warm off-white>;
  --color-ink: <near-black>;
  --color-accent: <brand color>;
  --color-accent-soft: <light tint>;
  --color-accent-ink: <text on accent-soft>;
  --color-rule: <border color>;
  --color-rel-family: ...;
  --color-rel-romance: ...;
  --color-rel-ally: ...;
  --color-rel-enemy: ...;
  /* font, radius tokens */
}
.dark { /* overrides */ }
@layer base { /* body bg, text, font-family, selection */ }
```
Wartości z `svelte(wideframe)/handoff/01-design-system.md`.

**`src/routes/+layout.svelte`:**
```svelte
<script lang="ts">
  import '../app.css';
  import { ModeWatcher } from 'mode-watcher';
  import { Toaster } from '$lib/components/ui/sonner';
  import AppShell from '$lib/components/shell/AppShell.svelte';
  let { children } = $props();
</script>
<ModeWatcher />
<Toaster />
<AppShell>{@render children()}</AppShell>
```

### Krok 2.7.3 — AppShell + UserMenu

**`src/lib/components/shell/AppShell.svelte`:**
- Sticky top nav: `bg-paper/80 backdrop-blur border-b border-rule`
- Brand (link `/`), nav links (Discover → `/discover`, My shelf → `/u/me/shelf`)
- Search button → otwiera shadcn `Command` palette (`Cmd+K`)
- Prawy side: `UserMenu`
- `<slot />` / `{@render children()}` dla treści strony

**`src/lib/components/shell/UserMenu.svelte`:**
- `Avatar` trigger → `DropdownMenu`
- Opcje: Profile (`/u/[handle]`), Settings (`/settings`), Sign out (form action)
- Dark mode toggle via `setMode` z `mode-watcher`

### Krok 2.7.4 — Atomy

**`src/lib/components/book/BookCover.svelte`:**
```svelte
<script lang="ts">
  import type { Book } from '$lib/types';
  interface Props { book: Book; size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl'; }
  let { book, size = 'md' }: Props = $props();
</script>
```
- aspect-2/3, `<img loading="lazy">`, fallback striped placeholder gdy `cover_url` null

**`src/lib/components/book/BookCard.svelte`** — `BookCover` + title + author + genre badge

**`src/lib/components/book/BookGrid.svelte`** — responsive grid `grid-cols-2 md:grid-cols-3 lg:grid-cols-5`, props: `books: Book[]`

**`src/lib/components/book/BookGridSkeleton.svelte`** — 10x `<Skeleton>` w tym samym grid

**`src/lib/components/character/CharacterAvatar.svelte`** — kołowy, fallback monogram inicjałów

**`src/lib/components/character/RelationBadge.svelte`** — kolorowy dot + label wg `kind`, kolor z `--color-rel-*`

**`src/lib/components/ai/AIBadge.svelte`** — `Sparkles` + "AI" pill

**`src/lib/components/ai/AIPanel.svelte`** — dashed accent border wrapper + title slot

### Krok 2.7.5 — Typy i schematy Zod

**`src/lib/types/book.ts`** — `Book`, `Author`, `Genre`, `Review`, `ShelfEntry`, `PaginatedList<T>`, `AIExtractionStatus` (wg `handoff/02-data-model.md`)

**`src/lib/types/character.ts`** — `BookCharacter`, `CharacterRole`, `Relation`, `RelationType`

**`src/lib/types/user.ts`** — `User`, `UserSettings`, `Visibility`

**`src/lib/types/ai.ts`** — `AIExtraction`, `ExtractionStatus`, `ConfidenceSummary`

**`src/lib/types.ts`** — re-export wszystkiego

**`src/lib/schemas/review.ts`:**
```ts
import { z } from 'zod';
export const reviewSchema = z.object({
  rating: z.number().int().min(1).max(5),
  body: z.string().min(10).max(5000)
});
export type ReviewSchema = typeof reviewSchema;
```

**`src/lib/schemas/user.ts`** — changeEmail, changePassword, updateProfile schemas

**`src/lib/schemas/settings.ts`** — AIPreferences, visibilitySchema

### Krok 2.7.6 — API wrappery

> Sprawdź kształt endpointów w `svelte(wideframe)/handoff/03-api-contract.md` i `backend-django/` przed pisaniem wrapperów.

**`src/lib/api/books.ts`:**
```ts
export async function listBooks(fetch: typeof window.fetch, params?: {...}) { ... }
export async function getBook(fetch: typeof window.fetch, slug: string) { ... }
export async function getReviews(fetch: typeof window.fetch, slug: string, page = 1) { ... }
export async function createReview(fetch: typeof window.fetch, slug: string, data: ReviewInput) { ... }
export async function searchByCharacter(fetch: typeof window.fetch, name: string) { ... }
```

**`src/lib/api/characters.ts`** — `getBookCharacters`, `getBookRelations`, `getCharacter`, `updateCharacter`

**`src/lib/api/ai.ts`** — `runExtraction`, `getExtraction`, `verifyCharacter`, `verifyRelation`, `rejectCharacter`

**`src/lib/api/shelves.ts`** — `getMyShelves`, `addToShelf`, `removeFromShelf`

### Krok 2.7.7 — Mock fixtures

**`src/lib/api/__fixtures__/`:**
- `books.ts` — 2 książki (jedna z extraction `ready`, jedna `none`)
- `characters.ts` — 8 postaci dla pierwszej książki
- `relations.ts` — 6 relacji między postaciami
- `user.ts` — 1 user + 1 settings
- `extraction.ts` — 1 extraction record

**`src/routes/api/__mock/[...rest]/+server.ts`** — routing fixture wg ścieżki gdy `PUBLIC_API_URL === 'MOCK'`

**`.env.example`:**
```
PUBLIC_API_URL=MOCK
# PUBLIC_API_URL=http://localhost:8000
```

### Krok 2.7.8 — Trasa `/discover`

**`src/routes/discover/+page.server.ts`:**
```ts
import type { PageServerLoad } from './$types';
import { listBooks } from '$lib/api/books';

export const load: PageServerLoad = async ({ fetch, url }) => {
  const q = url.searchParams.get('q') ?? '';
  const genre = url.searchParams.get('genre') ?? '';
  const sort = url.searchParams.get('sort') ?? 'title';
  const page = Number(url.searchParams.get('page') ?? 1);
  const { data, error } = await listBooks(fetch, { q, genre, sort, page });
  if (error) throw error(500, error.detail);
  return { books: data };
};
```

**`src/routes/discover/+page.svelte`** — `BookGrid` + `FilterBar`, URL jako source of truth, `goto` przy zmianie filtrów, "Load more" paginacja, empty state

**`src/lib/components/discover/FilterBar.svelte`** — genre `Select`, sort `Select`, `ContainsCharacterCombobox`

**`src/lib/components/discover/ContainsCharacterCombobox.svelte`** — shadcn `Command`, debounce 250ms, ustawia `?character=` przez `goto`

### Lint gate 2.7

```bash
cd svelte-frontend && pnpm check && pnpm lint && pnpm build
```
**Musi przejść 0 errors/0 warnings przed startem 2.8.**

---

## Faza 2.8 — Books, AI, Characters

**Branch:** `phase/2.8-svelte-books` (od merge 2.7)
**Subagent:** nowy agent po merge PR 2.7

### Krok 2.8.1 — Book detail `/books/[slug]`

**`src/routes/books/[slug]/+page.server.ts`:**
```ts
import type { PageServerLoad } from './$types';
export const load: PageServerLoad = async ({ fetch, params }) => {
  const [bookRes, reviewsRes] = await Promise.all([
    getBook(fetch, params.slug),
    getReviews(fetch, params.slug)
  ]);
  if (bookRes.error) throw error(bookRes.error.status, bookRes.error.detail);
  return { book: bookRes.data, reviews: reviewsRes.data };
};
```

**`src/routes/books/[slug]/+page.svelte`** — 3-kolumnowy layout:
- Col 1: `BookCover xl` + `BookActions`
- Col 2: `BookHeader` + `BookDescription` + `ReviewList` + `ReviewComposer`
- Col 3: `AICastPanel` (sticky)
- Stack poniżej 1024px

**Komponenty:**
- `BookHeader.svelte` — title (display font), authors, year, genres badges
- `BookActions.svelte` — split button "Want to read" + `DropdownMenu` (Want/Reading/Read). Optimistic: `addToShelf` → update local `$state` → rollback on error + toast
- `BookDescription.svelte` — `font-display italic` dla pulled quotes
- `ReviewList.svelte` — lista + paginacja (load more)
- `ReviewItem.svelte` — avatar + handle + rating + body + date
- `ReviewComposer.svelte` — superforms + zod `reviewSchema`, `use:enhance`, inline validation errors, toast "Review posted" on success
- `RatingStars.svelte` — props: `value`, `readonly?: boolean`, interactive click + hover

### Krok 2.8.2 — AI Cast Panel

**`src/lib/components/ai/AICastPanel.svelte`** — state machine:
```ts
type PanelState = 'idle' | 'pending' | 'ready' | 'failed';
let state: PanelState = $state('idle');
let characters = $state<Character[]>([]);
```

- **idle:** explainer copy + "Generate ✨" button → `runExtraction()` → state `pending`
- **pending:** 8x `CharacterRow` skeleton + pulsing dot "Reading the book…"
- **ready:** lista `CharacterRow`, "See all (n)" link, chips: `Open relation graph`, `Ask about this book` (placeholder toast)
- **failed:** error message + Retry button

**`src/lib/components/ai/CharacterRow.svelte`:**
- Avatar + name + role + `AIBadge` gdy `source === 'ai'`
- `MoreVertical` menu: Verify → `verifyCharacter(id)` + optimistic badge removal; Reject → `Dialog` z 4 opcjami

**`src/lib/components/ai/RelationGraphDialog.svelte`:**
- Pełnoekranowy `Dialog`
- `@xyflow/svelte` `SvelteFlow` + `Background` + `Controls` + `MiniMap`
- Lazy load: `const { SvelteFlow } = await import('@xyflow/svelte')` w `$effect`
- Node = `CharacterAvatar md` + label
- Edge `stroke` = `var(--color-rel-{kind})`, `stroke-dasharray` dla enemy, `stroke-width: 3` dla romance
- Klik na node → `goto(/books/[slug]/characters/[charId])`
- Filter chips: all / family / romance / ally / enemy
- **Keyboard fallback:** `<ul aria-label="Relations list">` obok grafu (widoczny dla screen readerów)

### Krok 2.8.3 — Character pages

**`src/routes/books/[slug]/characters/+page.server.ts`** — `getBookCharacters(fetch, slug)`

**`src/routes/books/[slug]/characters/+page.svelte`:**
- 2-kolumnowy: lista po lewej (search + filter by role), detail po prawej (`?focus=charId`)
- Mobile: stack (lista → detail jako osobna sekcja)

**`src/routes/books/[slug]/characters/[charId]/+page.server.ts`** — `getCharacter(fetch, charId)` + `getBookRelations(fetch, slug, charId)`

**`src/routes/books/[slug]/characters/[charId]/+page.svelte`:**
- Header: `CharacterAvatar xl`, name (display font), AKA, role, tag badges
- 2-kolumnowy: bio+quotes | relations+appearances
- Edit button → shadcn `Sheet` z superforms `updateCharacter`, tylko dla admina (disabled + tooltip dla pozostałych)

### Lint gate 2.8

```bash
cd svelte-frontend && pnpm check && pnpm lint && pnpm build
```
**0 errors/0 warnings przed startem 2.9.**

---

## Faza 2.9 — Profile, Settings, Polish

**Branch:** `phase/2.9-svelte-profile` (od merge 2.8)
**Subagent:** nowy agent po merge PR 2.8

### Krok 2.9.1 — Auth routes

**`src/routes/login/+page.server.ts`** — action: `POST /api/auth/login/` → ustaw cookie → redirect `/discover`

**`src/routes/login/+page.svelte`** — superforms + zod (email required, password required), inline errors

**`src/routes/signup/+page.server.ts`** — action: `POST /api/auth/register/`

**`src/routes/signup/+page.svelte`** — superforms + zod

Oba routes: `export const prerender = false`, `export const ssr = true`.

### Krok 2.9.2 — Profil `/u/[handle]`

**`src/routes/u/[handle]/+page.server.ts`** — `getUser(fetch, handle)` + `getUserShelves(fetch, handle)`

**`src/routes/u/[handle]/+page.svelte`:**
- Header: avatar, display name, handle, joined date, top genres
- Reading challenge: `<progress>` z `user.reading_goal`
- Recently read: flex row 6 `BookCover sm` ze `RatingStars readonly`
- AI sidecar (prawa kolumna, dashed accent border): recent extractions list + inline settings preview (linki do `/settings/ai`)
- Visibility guard: gdy `profile_public === false` i viewer nie jest właścicielem → "This profile is private."

### Krok 2.9.3 — Settings

**`src/routes/settings/+layout.svelte`** — left rail nav (active: `bg-ink text-paper rounded-md`):
Account / Profile & privacy / Notifications / AI preferences / Data & export / Danger zone

**`src/routes/settings/+page.svelte`** — Account:
- Avatar: hidden `<input type="file" accept="image/*">` + dropzone div + crop `Dialog` + `uploadAvatar`
- Display name / username — superforms, auto-save `on:blur`
- Email — superforms + verify-email `Dialog`
- Password (current/new/confirm) + strength bar (zxcvbn-like, czysta heurystyka bez lib)

**`src/routes/settings/profile/+page.svelte`** — Profile & privacy:
- `RadioGroup` 3 karty: Public / Friends only / Private
- `Switch` togglesy: real-name, activity feed, AI-learning, SEO indexing

**`src/routes/settings/notifications/+page.svelte`** — email + push per event type

**`src/routes/settings/ai/+page.svelte`** — tone / spoiler limit / cite quotes / chat retention slider

**`src/routes/settings/data/+page.svelte`:**
- Export data → `GET /users/me/export` → Dialog z `download_url` + copy button
- Pause account toggle
- **Delete account:** czerwony outlined button → `Dialog` wymagający wpisania handle → `DELETE /users/me` → `invalidateAll()` → `goto('/')`

### Krok 2.9.4 — Polish pass

**Empty states:** każda lista ma: ikona + friendly copy + CTA. Komponenty:
- `EmptyState.svelte` — props: `icon`, `title`, `description`, `cta?: { label, href }`

**Error boundaries:**
- `src/routes/+error.svelte` — literacka linia dla 404, retry + go-home dla reszty
- `src/routes/books/[slug]/+error.svelte` — book-specific
- `src/routes/u/[handle]/+error.svelte`

**A11y:**
- Wszystkie `<img>` z `alt`
- Dekoracyjne ikony: `aria-hidden="true"`
- Focus ring: `focus-visible:ring-2 focus-visible:ring-accent` na wszystkich interactive
- Skip link: `<a href="#main-content" class="sr-only focus:not-sr-only">` w `AppShell`
- Dialogi i dropdown: focus trap (shadcn obsługuje automatycznie przez bits-ui)

**Performance:**
- `<img loading="lazy">` na coverach poniżej fold
- `@xyflow/svelte` — dynamic import w `$effect` (tylko na book detail)
- `export const prerender = true` na `/login`, `/signup`

**Meta per route:**
```svelte
<svelte:head>
  <title>{book.title} — Storyshelf</title>
  <meta name="description" content={book.description?.slice(0, 160)} />
  <meta property="og:title" content={book.title} />
  <meta property="og:image" content={book.cover_url ?? ''} />
</svelte:head>
```

**`static/manifest.webmanifest`** — name, short_name, icons, display: standalone, theme_color

### Krok 2.9.5 — Finalna weryfikacja

```bash
cd svelte-frontend
pnpm check      # 0 errors
pnpm lint       # 0 warnings
pnpm build      # bez błędów
```

Ręczny smoke test w MOCK mode:
- [ ] `/discover` — grid, filtry, dark mode
- [ ] `/books/dune` — 3 kolumny, AI panel idle → generate → ready, graf relacji
- [ ] `/books/dune/characters` — lista + detail
- [ ] `/u/demo` — profil, visibility guard
- [ ] `/settings` — account, profile, data, delete flow (do Dialog, nie confirmuj)
- [ ] Keyboard: Tab przez całą Discover → Book → AI panel
- [ ] Mobile 375px: stack layout, nav działa

---

## Mapa plików po implementacji

```
svelte-frontend/src/
  app.css                              ← @theme block, .dark, @layer base
  app.html                             ← Google Fonts links
  lib/
    api/
      _client.ts                       ← już istnieje
      books.ts
      characters.ts
      ai.ts
      shelves.ts
      user.ts                          ← już istnieje
      __fixtures__/
        books.ts / characters.ts / relations.ts / user.ts / extraction.ts
    components/
      ui/                              ← shadcn-svelte (auto-generated)
      shell/
        AppShell.svelte
        UserMenu.svelte
      book/
        BookCover.svelte / BookCard.svelte / BookGrid.svelte / BookGridSkeleton.svelte
        BookHeader.svelte / BookActions.svelte / BookDescription.svelte
        ReviewList.svelte / ReviewItem.svelte / ReviewComposer.svelte / RatingStars.svelte
      character/
        CharacterAvatar.svelte / RelationBadge.svelte
      ai/
        AIBadge.svelte / AIPanel.svelte / AICastPanel.svelte
        CharacterRow.svelte / RelationGraphDialog.svelte
      discover/
        FilterBar.svelte / ContainsCharacterCombobox.svelte
      EmptyState.svelte
    schemas/
      review.ts / user.ts / settings.ts
    types/
      book.ts / character.ts / user.ts / ai.ts
    types.ts
    utils.ts                           ← już istnieje
    config.ts                          ← już istnieje
  routes/
    +layout.svelte / +layout.server.ts / +page.svelte / +page.server.ts / +error.svelte
    api/__mock/[...rest]/+server.ts
    discover/+page.svelte / +page.server.ts
    books/[slug]/+page.svelte / +page.server.ts / +error.svelte
    books/[slug]/characters/+page.svelte / +page.server.ts
    books/[slug]/characters/[charId]/+page.svelte / +page.server.ts
    u/[handle]/+page.svelte / +page.server.ts / +error.svelte
    settings/+layout.svelte
    settings/+page.svelte
    settings/profile/+page.svelte
    settings/notifications/+page.svelte
    settings/ai/+page.svelte
    settings/data/+page.svelte
    login/+page.svelte / +page.server.ts
    signup/+page.svelte / +page.server.ts
static/
  manifest.webmanifest
```

---

## Checklisty subagentów

### Subagent 2.7 — przed startem sprawdź:
- [ ] `context7` `/websites/svelte_dev_kit` — aktualna składnia `$props()`, `load`, form actions
- [ ] `context7` `/llmstxt/shadcn-svelte_llms_txt` — aktualny `shadcn-svelte init` + lista komponentów
- [ ] Odczytaj `svelte(wideframe)/handoff/01-design-system.md` — tokeny kolorów
- [ ] Odczytaj `svelte(wideframe)/handoff/03-api-contract.md` — kształt endpointów

### Subagent 2.8 — przed startem sprawdź:
- [ ] `context7` `/ciscoheat/sveltekit-superforms` — aktualna składnia `superForm` + `zod4Client`
- [ ] Odczytaj `svelte(wideframe)/handoff/05-ai-features.md` — AI panel spec
- [ ] Odczytaj `svelte(wideframe)/handoff/04-routes-and-screens.md` → Book detail, Character pages

### Subagent 2.9 — przed startem sprawdź:
- [ ] `context7` `/websites/svelte_dev_kit` — `prerender`, OG meta, `svelte:head`
- [ ] Odczytaj `svelte(wideframe)/handoff/04-routes-and-screens.md` → Profile, Settings

---

## Definition of Done

| Check | Standard |
|-------|----------|
| `pnpm check` | 0 TypeScript errors |
| `pnpm lint` | 0 ESLint/Prettier warnings |
| `pnpm build` | build bez błędów |
| MOCK mode | wszystkie trasy renderują, brak console errors |
| Real backend | `/discover` + `/books/[slug]` działają z lokalnym Django |
| Dark mode | brak flash przy reload, poprawne kolory |
| Keyboard | Discover → Book → AI panel → Profile → Settings bez myszy |
| Mobile | 375px stack layout, nawigacja działa |
