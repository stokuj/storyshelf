# Phase 2.2 — AI Extraction API — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wystawić HTTP API do triggera/odczytu ekstrakcji AI (admin-only), dodać `slug`/`source`/`confidence`/`is_hidden` na `BookCharacter`, `ai_extraction_status` na `Book`, i przerobić `analyse_book` na upsert zachowujący historię admina między re-runami.

**Architecture:** Sześć niezależnych warstw budowanych od modeli ku endpointom. Najpierw migracje (Task 1–2), potem refaktor taska Celery (Task 3), potem nowe views/serializers (Task 4–5), na końcu admin i cleanup (Task 6–7). Każdy task produkuje działający stan — można je wdrażać incrementalnie. Testy TDD na każdej warstwie.

**Tech Stack:** Django 6, DRF 3.17, Celery (shared_task), django.utils.text.slugify, pytest-django, `DJANGO_ENV=dev uv run python manage.py test`.

---

## Pliki do zmiany

| Plik | Akcja |
|---|---|
| `backend-django/analysis/models.py` | dodać `Source` enum + `slug`/`source`/`confidence`/`is_hidden` na `BookCharacter` + `save()` |
| `backend-django/analysis/migrations/0004_bookcharacter_ai_fields.py` | NOWA — AddField ×4 + RunPython backfill + UniqueConstraint |
| `backend-django/books/models.py` | dodać `AIExtractionStatus` enum + 4 pola status/timestamps/failure_reason |
| `backend-django/books/migrations/0005_book_ai_extraction_status.py` | NOWA (zależy od Phase 2.1 która dodaje `0004_book_slug`) |
| `backend-django/analysis/tasks.py` | refaktor `analyse_book`: upsert zamiast delete+create + status updates + try/except |
| `backend-django/analysis/serializers.py` | NOWY — `AIExtractionSerializer`, rozszerzony `BookCharacterSerializer` |
| `backend-django/analysis/views.py` | NOWY — `AIExtractionTriggerView`, `AIExtractionRetrieveView`, `BookCharacterHideView` |
| `backend-django/analysis/urls.py` | NOWY — 3 routes |
| `backend-django/config/urls.py` | dodać `path("api/", include("analysis.urls"))` |
| `backend-django/books/serializers.py:8-13` | rozszerzyć `BookCharacterSerializer` o nowe pola; filtrować `is_hidden` dla non-admin |
| `backend-django/books/views.py` | `BookRetrieveView`: `Prefetch("characters")` z `is_hidden=False` dla non-admin |
| `backend-django/analysis/admin.py` | `BookCharacterAdmin`: `list_display`/`list_filter` + akcje `mark_hidden`/`mark_visible` |
| `backend-django/analysis/tests/test_models.py` | NOWY — slug generation + collision + `save()` |
| `backend-django/analysis/tests/test_tasks.py` | rozszerzyć — idempotentność upsert + status flow |
| `backend-django/analysis/tests/test_views.py` | NOWY — permissions + trigger idempotence + hide flow |
| `docs/api/openapi.yml` | regeneracja |

---

## Out of scope

- `POST /books/:id/ai/extract/refresh` (force re-run endpoint) — WYCIĘTE z MVP
- `POST /books/:id/ai/extraction/:characterId/verify` i `/reject` — WYCIĘTE z MVP
- `POST /books/:id/ai/ask` (SSE chat) — WYCIĘTE z MVP (złamie ADR-002 + wymaga ASGI)
- Cross-book Character (globalny model) — ADR-003
- `source`/`confidence` na `CharacterRelationship` — Phase 4
- Webhooks/SSE statusu — Svelte polluje GET co 3s
- `role`/`archetype`/`short_bio`/`long_bio` na Character — brak w backendzie i poza MVP
- `also_known_as` (aliasy) — Phase 2.5

---

## Task 1: `BookCharacter` — nowe pola + migracja

**Files:**
- Modify: `backend-django/analysis/models.py`
- Create: `backend-django/analysis/migrations/0004_bookcharacter_ai_fields.py`
- Create: `backend-django/analysis/tests/test_models.py`

- [ ] **Krok 1: Napisz failing testy modelu**

Utwórz `backend-django/analysis/tests/test_models.py`:

```python
from django.test import TestCase
from django.utils.text import slugify

from analysis.models import BookCharacter
from books.models import Book


class BookCharacterSlugTest(TestCase):
    def setUp(self):
        self.book = Book.objects.create(title="Test Book")

    def test_slug_generated_on_create(self):
        char = BookCharacter.objects.create(book=self.book, name="Hermione Granger", mention_count=5)
        self.assertEqual(char.slug, "hermione-granger")

    def test_slug_collision_within_book_adds_suffix(self):
        BookCharacter.objects.create(book=self.book, name="Hermione", mention_count=5)
        char2 = BookCharacter.objects.create(book=self.book, name="Hermione", mention_count=3)
        self.assertNotEqual(char2.slug, "hermione")
        self.assertTrue(char2.slug.startswith("hermione-"))

    def test_slug_not_changed_on_update(self):
        char = BookCharacter.objects.create(book=self.book, name="Gandalf", mention_count=10)
        original_slug = char.slug
        char.mention_count = 20
        char.save()
        char.refresh_from_db()
        self.assertEqual(char.slug, original_slug)

    def test_default_source_is_ai(self):
        char = BookCharacter.objects.create(book=self.book, name="Frodo", mention_count=8)
        self.assertEqual(char.source, "ai")

    def test_default_is_hidden_false(self):
        char = BookCharacter.objects.create(book=self.book, name="Samwise", mention_count=6)
        self.assertFalse(char.is_hidden)

    def test_confidence_nullable(self):
        char = BookCharacter.objects.create(book=self.book, name="Aragorn", mention_count=4)
        self.assertIsNone(char.confidence)

    def test_unique_slug_per_book_not_global(self):
        book2 = Book.objects.create(title="Other Book")
        BookCharacter.objects.create(book=self.book, name="Legolas", mention_count=3)
        # Same slug in different book — allowed
        char2 = BookCharacter.objects.create(book=book2, name="Legolas", mention_count=2)
        self.assertEqual(char2.slug, "legolas")
```

- [ ] **Krok 2: Uruchom test — powinien failować**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test analysis.tests.test_models -v 2
```

Oczekiwane: `ImportError` lub `AttributeError` — pola `slug`/`source`/`is_hidden`/`confidence` nie istnieją.

- [ ] **Krok 3: Zaktualizuj `analysis/models.py`**

Zastąp całość `backend-django/analysis/models.py`:

```python
from django.core.validators import MaxValueValidator, MinValueValidator
from django.db import models
from django.utils.text import slugify


def _generate_character_slug(name: str, book_id: int) -> str:
    """Generate unique slug per book for BookCharacter."""
    base = slugify(name)[:200] or "character"
    slug = base
    counter = 2
    while BookCharacter.objects.filter(slug=slug, book_id=book_id).exists():
        slug = f"{base}-{counter}"
        counter += 1
    return slug


class BookCharacter(models.Model):
    class Source(models.TextChoices):
        HUMAN = "human"
        AI = "ai"
        AI_VERIFIED = "ai-verified"

    book = models.ForeignKey(
        "books.Book", on_delete=models.CASCADE, related_name="characters"
    )
    name = models.CharField(max_length=255)
    slug = models.SlugField(max_length=255, blank=True, default="")
    description = models.TextField(null=True, blank=True)
    mention_count = models.IntegerField(default=0)
    source = models.CharField(
        max_length=20, choices=Source.choices, default=Source.AI
    )
    confidence = models.FloatField(
        null=True,
        blank=True,
        validators=[MinValueValidator(0.0), MaxValueValidator(1.0)],
    )
    is_hidden = models.BooleanField(default=False)

    class Meta:
        constraints = [
            models.UniqueConstraint(fields=["name", "book"], name="unique_character_per_book"),
            models.UniqueConstraint(fields=["slug", "book"], name="unique_character_slug_per_book"),
        ]

    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = _generate_character_slug(self.name, self.book_id)
        super().save(*args, **kwargs)

    def __str__(self):
        return self.name


class BookPlace(models.Model):
    book = models.ForeignKey(
        "books.Book", on_delete=models.CASCADE, related_name="places"
    )
    name = models.CharField(max_length=255)
    description = models.TextField(null=True, blank=True)
    mention_count = models.IntegerField(default=0)

    class Meta:
        constraints = [
            models.UniqueConstraint(fields=["name", "book"], name="unique_place_per_book"),
        ]

    def __str__(self):
        return self.name


class BookOrganization(models.Model):
    book = models.ForeignKey(
        "books.Book", on_delete=models.CASCADE, related_name="organizations"
    )
    name = models.CharField(max_length=255)
    description = models.TextField(null=True, blank=True)
    mention_count = models.IntegerField(default=0)

    class Meta:
        constraints = [
            models.UniqueConstraint(fields=["name", "book"], name="unique_org_per_book"),
        ]

    def __str__(self):
        return self.name


class CharacterRelationship(models.Model):
    class RelationType(models.TextChoices):
        PARENT_OF = "parent_of"
        SIBLING_OF = "sibling_of"
        SPOUSE_OF = "spouse_of"
        ANCESTOR_OF = "ancestor_of"
        FRIEND_OF = "friend_of"
        ENEMY_OF = "enemy_of"
        RIVAL_OF = "rival_of"
        MENTOR_OF = "mentor_of"
        LOVER_OF = "lover_of"
        ADMIRES = "admires"
        RULER_OF = "ruler_of"
        COMMANDS = "commands"
        SERVES = "serves"
        MEMBER_OF_FACTION = "member_of_faction"
        FIGHTS_AGAINST = "fights_against"
        PROTECTS = "protects"
        BETRAYS = "betrays"
        SAVES = "saves"
        HUNTS = "hunts"
        KNOWS_SECRET_OF = "knows_secret_of"
        MANIPULATES = "manipulates"
        DECEIVES = "deceives"
        CREATOR_OF = "creator_of"
        CLONE_OF = "clone_of"

    from_character = models.ForeignKey(
        BookCharacter, on_delete=models.CASCADE, related_name="relations_from"
    )
    to_character = models.ForeignKey(
        BookCharacter, on_delete=models.CASCADE, related_name="relations_to"
    )
    relation_type = models.CharField(max_length=30, choices=RelationType.choices)
    book = models.ForeignKey(
        "books.Book", on_delete=models.CASCADE, related_name="character_relationships"
    )

    class Meta:
        constraints = [
            models.UniqueConstraint(
                fields=["from_character", "to_character", "book"],
                name="unique_character_relation_per_book",
            ),
        ]

    def __str__(self):
        return f"{self.from_character.name} {self.relation_type} {self.to_character.name}"
```

- [ ] **Krok 4: Utwórz migrację**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py makemigrations analysis --name bookcharacter_ai_fields
```

Oczekiwane: tworzy `analysis/migrations/0004_bookcharacter_ai_fields.py`.

- [ ] **Krok 5: Edytuj migrację — dodaj RunPython backfill**

Zastąp wygenerowany plik `backend-django/analysis/migrations/0004_bookcharacter_ai_fields.py`:

```python
from django.db import migrations, models
from django.utils.text import slugify
import django.core.validators


def backfill_character_slugs(apps, schema_editor):
    BookCharacter = apps.get_model("analysis", "BookCharacter")
    # Group by book to handle per-book uniqueness
    from collections import defaultdict
    by_book = defaultdict(list)
    for char in BookCharacter.objects.all().order_by("id"):
        by_book[char.book_id].append(char)

    for book_id, chars in by_book.items():
        used_slugs = set()
        for char in chars:
            base = slugify(char.name)[:200] or "character"
            slug = base
            counter = 2
            while slug in used_slugs:
                slug = f"{base}-{counter}"
                counter += 1
            used_slugs.add(slug)
            char.slug = slug
            char.save(update_fields=["slug"])


class Migration(migrations.Migration):
    dependencies = [
        ("analysis", "0003_alter_bookcharacter_unique_together_and_more"),
    ]

    operations = [
        migrations.AddField(
            model_name="bookcharacter",
            name="slug",
            field=models.SlugField(blank=True, default="", max_length=255),
        ),
        migrations.AddField(
            model_name="bookcharacter",
            name="source",
            field=models.CharField(
                choices=[("human", "Human"), ("ai", "Ai"), ("ai-verified", "Ai Verified")],
                default="ai",
                max_length=20,
            ),
        ),
        migrations.AddField(
            model_name="bookcharacter",
            name="confidence",
            field=models.FloatField(
                blank=True,
                null=True,
                validators=[
                    django.core.validators.MinValueValidator(0.0),
                    django.core.validators.MaxValueValidator(1.0),
                ],
            ),
        ),
        migrations.AddField(
            model_name="bookcharacter",
            name="is_hidden",
            field=models.BooleanField(default=False),
        ),
        migrations.RunPython(backfill_character_slugs, reverse_code=migrations.RunPython.noop),
        migrations.AddConstraint(
            model_name="bookcharacter",
            constraint=models.UniqueConstraint(
                fields=["slug", "book"], name="unique_character_slug_per_book"
            ),
        ),
    ]
```

- [ ] **Krok 6: Uruchom migrację**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py migrate
```

Oczekiwane: migracja przechodzi bez błędów.

- [ ] **Krok 7: Uruchom testy modelu — green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test analysis.tests.test_models -v 2
```

Oczekiwane: 7 PASS.

- [ ] **Krok 8: Pełen suite**

```bash
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS.

- [ ] **Krok 9: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/analysis/models.py backend-django/analysis/migrations/0004_bookcharacter_ai_fields.py backend-django/analysis/tests/test_models.py
git commit -m "feat(analysis): add slug/source/confidence/is_hidden to BookCharacter"
```

---

## Task 2: `Book.ai_extraction_status` — pola + migracja

**Files:**
- Modify: `backend-django/books/models.py`
- Create: `backend-django/books/migrations/0005_book_ai_extraction_status.py`

**Zależność:** Zakłada że Phase 2.1 (migration `0004_book_slug`) jest już w bazie. Jeśli nie — Django zrobi automatycznie poprawną kolejność przez `dependencies`.

- [ ] **Krok 1: Dodaj `AIExtractionStatus` i pola do `books/models.py`**

Na początku `backend-django/books/models.py`, po istniejących importach, dodaj:

```python
from django.utils import timezone
```

Przed klasą `Book` (lub po `from django.core.validators import MinValueValidator`) dodaj enum:

```python
class AIExtractionStatus(models.TextChoices):
    NONE = "none"
    PENDING = "pending"
    RUNNING = "running"
    DONE = "done"
    FAILED = "failed"
```

W klasie `Book`, po linii `ratings_count` (linia 21) dodaj pola:

```python
    ai_extraction_status = models.CharField(
        max_length=20,
        choices=AIExtractionStatus.choices,
        default=AIExtractionStatus.NONE,
    )
    ai_extraction_started_at = models.DateTimeField(null=True, blank=True)
    ai_extraction_finished_at = models.DateTimeField(null=True, blank=True)
    ai_extraction_failure_reason = models.TextField(blank=True, default="")
```

- [ ] **Krok 2: Utwórz migrację**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py makemigrations books --name book_ai_extraction_status
```

Oczekiwane: tworzy `books/migrations/0005_book_ai_extraction_status.py`.

- [ ] **Krok 3: Edytuj migrację — dodaj RunPython backfill dla istniejących danych**

Otwórz wygenerowany plik i dodaj `RunPython` po `AddField` operacjach:

```python
from django.db import migrations, models


def backfill_extraction_status(apps, schema_editor):
    Book = apps.get_model("books", "Book")
    # Books with characters already extracted → DONE
    done_ids = (
        Book.objects.filter(characters__isnull=False)
        .values_list("id", flat=True)
        .distinct()
    )
    Book.objects.filter(id__in=done_ids).update(ai_extraction_status="done")
    # Rest stay "none" (default)


class Migration(migrations.Migration):
    dependencies = [
        ("books", "0004_book_slug"),  # Phase 2.1 migration
    ]

    operations = [
        migrations.AddField(
            model_name="book",
            name="ai_extraction_status",
            field=models.CharField(
                choices=[
                    ("none", "None"),
                    ("pending", "Pending"),
                    ("running", "Running"),
                    ("done", "Done"),
                    ("failed", "Failed"),
                ],
                default="none",
                max_length=20,
            ),
        ),
        migrations.AddField(
            model_name="book",
            name="ai_extraction_started_at",
            field=models.DateTimeField(blank=True, null=True),
        ),
        migrations.AddField(
            model_name="book",
            name="ai_extraction_finished_at",
            field=models.DateTimeField(blank=True, null=True),
        ),
        migrations.AddField(
            model_name="book",
            name="ai_extraction_failure_reason",
            field=models.TextField(blank=True, default=""),
        ),
        migrations.RunPython(backfill_extraction_status, reverse_code=migrations.RunPython.noop),
    ]
```

- [ ] **Krok 4: Uruchom migrację**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py migrate
```

Oczekiwane: exit 0.

- [ ] **Krok 5: Szybki smoke test w shellu Django**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py shell -c "
from books.models import Book, AIExtractionStatus
b = Book.objects.first()
if b:
    print('status:', b.ai_extraction_status)
    print('default OK:', b.ai_extraction_status == AIExtractionStatus.NONE or b.ai_extraction_status == AIExtractionStatus.DONE)
else:
    print('No books in DB (OK for test DB)')
"
```

- [ ] **Krok 6: Pełen suite**

```bash
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS.

- [ ] **Krok 7: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/books/models.py backend-django/books/migrations/0005_book_ai_extraction_status.py
git commit -m "feat(books): add ai_extraction_status + timestamps + failure_reason to Book"
```

---

## Task 3: Refaktor `analyse_book` — upsert + status tracking

**Files:**
- Modify: `backend-django/analysis/tasks.py`
- Modify: `backend-django/analysis/tests/test_tasks.py` (lub stwórz jeśli nie istnieje)

- [ ] **Krok 1: Sprawdź czy istnieje plik testów tasków**

```bash
ls /home/dv6/GitHub/storyshelf/backend-django/analysis/tests/
```

Jeśli `test_tasks.py` nie istnieje, utwórz:

```bash
touch /home/dv6/GitHub/storyshelf/backend-django/analysis/tests/__init__.py 2>/dev/null; touch /home/dv6/GitHub/storyshelf/backend-django/analysis/tests/test_tasks.py
```

- [ ] **Krok 2: Napisz failing testy idempotentności**

Dodaj (lub zastąp) zawartość `backend-django/analysis/tests/test_tasks.py`:

```python
from unittest.mock import MagicMock, patch

from django.test import TestCase

from analysis.models import BookCharacter
from analysis.tasks import analyse_book
from books.models import Book


class AnalyseBookIdempotencyTest(TestCase):
    def setUp(self):
        self.book = Book.objects.create(
            title="Test Book",
            text="Harry met Hermione. Harry said hello. Hermione replied.",
        )

    @patch("analysis.tasks.extract_entities_from_chunks")
    @patch("analysis.tasks.find_sentences_with_both_characters")
    @patch("analysis.tasks.relations_for_book")
    def test_upsert_preserves_is_hidden(self, mock_relations, mock_find, mock_extract):
        mock_extract.return_value = {
            "characters": {"Harry": 10, "Hermione": 8},
            "locations": {},
            "organizations": {},
        }
        mock_find.return_value = []
        mock_relations.delay = MagicMock()

        # First run
        analyse_book(self.book.id)
        char = BookCharacter.objects.get(book=self.book, name="Harry")
        char.is_hidden = True
        char.source = "ai-verified"
        char.save()

        # Reset text for re-run
        self.book.text = "Harry met Hermione. Harry said hello. Hermione replied."
        self.book.save()

        # Second run — same characters
        analyse_book(self.book.id)

        char.refresh_from_db()
        self.assertTrue(char.is_hidden, "is_hidden must survive re-run")
        self.assertEqual(char.source, "ai-verified", "source must survive re-run")

    @patch("analysis.tasks.extract_entities_from_chunks")
    @patch("analysis.tasks.find_sentences_with_both_characters")
    @patch("analysis.tasks.relations_for_book")
    def test_upsert_updates_mention_count(self, mock_relations, mock_find, mock_extract):
        mock_extract.return_value = {
            "characters": {"Gandalf": 5},
            "locations": {},
            "organizations": {},
        }
        mock_find.return_value = []
        mock_relations.delay = MagicMock()

        analyse_book(self.book.id)
        char = BookCharacter.objects.get(book=self.book, name="Gandalf")
        self.assertEqual(char.mention_count, 5)

        self.book.text = "Gandalf walked. Gandalf ran. Gandalf flew."
        self.book.save()
        mock_extract.return_value = {"characters": {"Gandalf": 20}, "locations": {}, "organizations": {}}

        analyse_book(self.book.id)
        char.refresh_from_db()
        self.assertEqual(char.mention_count, 20)

    @patch("analysis.tasks.extract_entities_from_chunks")
    @patch("analysis.tasks.find_sentences_with_both_characters")
    @patch("analysis.tasks.relations_for_book")
    def test_status_set_to_done_on_success(self, mock_relations, mock_find, mock_extract):
        mock_extract.return_value = {"characters": {"Alice": 3}, "locations": {}, "organizations": {}}
        mock_find.return_value = []
        mock_relations.delay = MagicMock()

        analyse_book(self.book.id)

        self.book.refresh_from_db()
        self.assertEqual(self.book.ai_extraction_status, "done")
        self.assertIsNotNone(self.book.ai_extraction_finished_at)

    @patch("analysis.tasks.extract_entities_from_chunks")
    def test_status_set_to_failed_on_exception(self, mock_extract):
        mock_extract.side_effect = RuntimeError("NER exploded")

        with self.assertRaises(RuntimeError):
            analyse_book(self.book.id)

        self.book.refresh_from_db()
        self.assertEqual(self.book.ai_extraction_status, "failed")
        self.assertIn("NER exploded", self.book.ai_extraction_failure_reason)
```

- [ ] **Krok 3: Uruchom testy — powinny failować**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test analysis.tests.test_tasks -v 2
```

Oczekiwane: FAIL — `analyse_book` robi delete+create, `is_hidden` nie przeżywa re-runu; brak statusu.

- [ ] **Krok 4: Zastąp `analysis/tasks.py`**

```python
import json
import logging

from celery import shared_task
from django.db import transaction
from django.utils import timezone
from django.utils.text import slugify

from .ner_engine import extract_entities_from_chunks
from .text_parser import find_sentences_with_both_characters

try:
    import openai

    from .llm_engine import llm_service
except ImportError:  # pragma: no cover
    openai = None  # type: ignore[assignment]
    llm_service = None  # type: ignore[assignment]

logger = logging.getLogger(__name__)


def _upsert_characters(book, entities: dict) -> list[str]:
    """Upsert BookCharacters from NER results. Returns list of created/updated names."""
    from .models import BookCharacter

    existing = {c.slug: c for c in BookCharacter.objects.filter(book=book)}
    char_names: list[str] = []
    used_slugs_this_run: set[str] = set()

    for name, count in entities.items():
        base = slugify(name)[:200] or "character"
        slug = base
        counter = 2
        # Deduplicate within this batch (NER may surface same name twice via aliasing)
        while slug in used_slugs_this_run:
            slug = f"{base}-{counter}"
            counter += 1
        used_slugs_this_run.add(slug)

        if slug in existing:
            char = existing[slug]
            char.mention_count = count
            char.save(update_fields=["mention_count"])
            logger.info("analyse_book: updated character '%s' (slug=%s)", name, slug)
        else:
            BookCharacter.objects.create(
                book=book,
                name=name,
                slug=slug,
                mention_count=count,
                source="ai",
                confidence=None,
                is_hidden=False,
            )
            logger.info("analyse_book: created character '%s' (slug=%s)", name, slug)
        char_names.append(name)

    logger.info(
        "analyse_book: book=%s — %d created/updated, %d untouched",
        book.id,
        len(char_names),
        len(existing) - len([s for s in used_slugs_this_run if s in existing]),
    )
    return char_names


@shared_task
def analyse_book(book_id: int):
    """Run NER on Book.text, upsert per-book entities (preserving admin flags), dispatch LLM task."""
    from books.models import Book

    from .models import BookOrganization, BookPlace

    try:
        book = Book.objects.get(id=book_id)
    except Book.DoesNotExist:
        logger.warning("analyse_book: Book %s does not exist", book_id)
        return
    if not book.text:
        return

    Book.objects.filter(id=book_id).update(
        ai_extraction_status="running",
        ai_extraction_started_at=timezone.now(),
        ai_extraction_failure_reason="",
    )

    try:
        full_text = book.text
        result = extract_entities_from_chunks(full_text)

        with transaction.atomic():
            char_names = _upsert_characters(book, result.get("characters", {}))

            # BookPlace / BookOrganization: simpler upsert by name (no source/is_hidden in MVP)
            for name, count in result.get("locations", {}).items():
                BookPlace.objects.update_or_create(
                    name=name, book=book, defaults={"mention_count": count}
                )
            for name, count in result.get("organizations", {}).items():
                BookOrganization.objects.update_or_create(
                    name=name, book=book, defaults={"mention_count": count}
                )

        pairs_data = find_sentences_with_both_characters(full_text, char_names)
        Book.objects.filter(id=book_id).update(text="")

        if pairs_data and len(char_names) >= 2:
            relations_for_book.delay(book_id, pairs_data)

        Book.objects.filter(id=book_id).update(
            ai_extraction_status="done",
            ai_extraction_finished_at=timezone.now(),
        )

    except Exception as exc:
        Book.objects.filter(id=book_id).update(
            ai_extraction_status="failed",
            ai_extraction_finished_at=timezone.now(),
            ai_extraction_failure_reason=str(exc)[:512],
        )
        raise


@shared_task
def relations_for_book(book_id: int, pairs_data: list[dict]):
    """Extract character relationships via LLM for each co-occurring pair.

    Errors per pair are logged and skipped — the task never retries.
    """
    if llm_service is None:
        logger.error("relations_for_book: llm_service unavailable, skipping book %s", book_id)
        return

    from .models import BookCharacter, CharacterRelationship

    valid_relation_types = {r.value for r in CharacterRelationship.RelationType}

    for item in pairs_data:
        pair = item["pair"]
        sentences = item["sentences"]
        if not sentences:
            continue
        try:
            result_json = llm_service.extract_relations(pair, sentences)
            result = json.loads(result_json)
        except (json.JSONDecodeError, openai.OpenAIError) as e:
            logger.warning("LLM error for pair %s: %s", pair, e, exc_info=True)
            continue

        for rel in result.get("relations", []):
            relation_type = rel.get("relation", "")
            if relation_type not in valid_relation_types:
                logger.warning("Unknown relation type '%s', skipping", relation_type)
                continue
            try:
                source = BookCharacter.objects.get(name=rel["source"], book_id=book_id)
                target = BookCharacter.objects.get(name=rel["target"], book_id=book_id)
                CharacterRelationship.objects.get_or_create(
                    from_character=source,
                    to_character=target,
                    book_id=book_id,
                    defaults={"relation_type": relation_type},
                )
            except BookCharacter.DoesNotExist:
                logger.warning("Character not found for relation: %s", rel)
                continue
```

- [ ] **Krok 5: Uruchom testy tasków — green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test analysis.tests.test_tasks -v 2
```

Oczekiwane: 4 PASS.

- [ ] **Krok 6: Pełen suite**

```bash
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS.

- [ ] **Krok 7: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/analysis/tasks.py backend-django/analysis/tests/test_tasks.py
git commit -m "refactor(analysis): upsert analyse_book preserves is_hidden/source + status tracking"
```

---

## Task 4: `analysis/serializers.py` + `analysis/views.py` + `analysis/urls.py` + routing

**Files:**
- Create: `backend-django/analysis/serializers.py`
- Create: `backend-django/analysis/views.py`
- Create: `backend-django/analysis/urls.py`
- Modify: `backend-django/config/urls.py`
- Create: `backend-django/analysis/tests/test_views.py`

- [ ] **Krok 1: Napisz failing testy endpointów**

Utwórz `backend-django/analysis/tests/test_views.py`:

```python
from django.test import TestCase
from rest_framework import status
from rest_framework.test import APIClient

from analysis.models import BookCharacter
from books.models import Book
from users.models import User


class AIExtractionPermissionsTest(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.book = Book.objects.create(title="AI Book", text="Some text about Alice and Bob.")
        self.admin = User.objects.create_superuser(
            email="admin@test.com", username="admin", password="adminpass"
        )
        self.user = User.objects.create_user(
            email="user@test.com", username="user", password="userpass"
        )

    def test_trigger_anonymous_returns_401(self):
        response = self.client.post(f"/api/books/{self.book.pk}/ai/extract/")
        self.assertEqual(response.status_code, status.HTTP_401_UNAUTHORIZED)

    def test_trigger_non_admin_returns_403(self):
        self.client.force_authenticate(user=self.user)
        response = self.client.post(f"/api/books/{self.book.pk}/ai/extract/")
        self.assertEqual(response.status_code, status.HTTP_403_FORBIDDEN)

    def test_retrieve_anonymous_returns_401(self):
        response = self.client.get(f"/api/books/{self.book.pk}/ai/extraction/")
        self.assertEqual(response.status_code, status.HTTP_401_UNAUTHORIZED)

    def test_retrieve_non_admin_returns_403(self):
        self.client.force_authenticate(user=self.user)
        response = self.client.get(f"/api/books/{self.book.pk}/ai/extraction/")
        self.assertEqual(response.status_code, status.HTTP_403_FORBIDDEN)

    def test_hide_anonymous_returns_401(self):
        char = BookCharacter.objects.create(book=self.book, name="Alice", mention_count=3)
        response = self.client.post(f"/api/books/{self.book.pk}/characters/{char.pk}/hide/")
        self.assertEqual(response.status_code, status.HTTP_401_UNAUTHORIZED)


class AIExtractionTriggerTest(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.admin = User.objects.create_superuser(
            email="admin2@test.com", username="admin2", password="adminpass"
        )
        self.client.force_authenticate(user=self.admin)
        self.book = Book.objects.create(title="Trigger Book", text="Alice and Bob walk.")

    @patch("analysis.views.analyse_book")
    def test_trigger_returns_202_and_sets_pending(self, mock_task):
        response = self.client.post(f"/api/books/{self.book.pk}/ai/extract/")
        self.assertEqual(response.status_code, status.HTTP_202_ACCEPTED)
        mock_task.delay.assert_called_once_with(self.book.pk)
        self.book.refresh_from_db()
        self.assertEqual(self.book.ai_extraction_status, "pending")

    @patch("analysis.views.analyse_book")
    def test_trigger_idempotent_when_pending(self, mock_task):
        Book.objects.filter(pk=self.book.pk).update(ai_extraction_status="pending")
        response = self.client.post(f"/api/books/{self.book.pk}/ai/extract/")
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        # Should NOT dispatch again
        mock_task.delay.assert_not_called()
        self.book.refresh_from_db()
        self.assertEqual(self.book.ai_extraction_status, "pending")

    @patch("analysis.views.analyse_book")
    def test_trigger_idempotent_when_running(self, mock_task):
        Book.objects.filter(pk=self.book.pk).update(ai_extraction_status="running")
        response = self.client.post(f"/api/books/{self.book.pk}/ai/extract/")
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        mock_task.delay.assert_not_called()

    def test_trigger_404_for_nonexistent_book(self):
        response = self.client.post("/api/books/99999/ai/extract/")
        self.assertEqual(response.status_code, status.HTTP_404_NOT_FOUND)

    def test_retrieve_returns_extraction_payload_shape(self):
        response = self.client.get(f"/api/books/{self.book.pk}/ai/extraction/")
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        for key in ("status", "characters", "relations", "covered_through", "confidence_summary", "failure_reason"):
            self.assertIn(key, response.data)

    def test_retrieve_covered_through_stub(self):
        response = self.client.get(f"/api/books/{self.book.pk}/ai/extraction/")
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        ct = response.data["covered_through"]
        self.assertIsNone(ct["chapter"])

    def test_retrieve_404_for_nonexistent_book(self):
        response = self.client.get("/api/books/99999/ai/extraction/")
        self.assertEqual(response.status_code, status.HTTP_404_NOT_FOUND)


class BookCharacterHideTest(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.admin = User.objects.create_superuser(
            email="admin3@test.com", username="admin3", password="adminpass"
        )
        self.client.force_authenticate(user=self.admin)
        self.book = Book.objects.create(title="Hide Book")
        self.char = BookCharacter.objects.create(
            book=self.book, name="Visible Character", mention_count=5
        )

    def test_hide_sets_is_hidden_true(self):
        response = self.client.post(
            f"/api/books/{self.book.pk}/characters/{self.char.pk}/hide/",
            {"hidden": True},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.char.refresh_from_db()
        self.assertTrue(self.char.is_hidden)

    def test_unhide_sets_is_hidden_false(self):
        self.char.is_hidden = True
        self.char.save()
        response = self.client.post(
            f"/api/books/{self.book.pk}/characters/{self.char.pk}/hide/",
            {"hidden": False},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.char.refresh_from_db()
        self.assertFalse(self.char.is_hidden)

    def test_hide_default_true_when_no_body(self):
        response = self.client.post(
            f"/api/books/{self.book.pk}/characters/{self.char.pk}/hide/",
        )
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.char.refresh_from_db()
        self.assertTrue(self.char.is_hidden)

    def test_hide_404_wrong_book(self):
        other_book = Book.objects.create(title="Other Book")
        response = self.client.post(
            f"/api/books/{other_book.pk}/characters/{self.char.pk}/hide/",
            {"hidden": True},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_404_NOT_FOUND)
```

- [ ] **Krok 2: Uruchom test — powinien failować (URL nie istnieje)**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test analysis.tests.test_views -v 2
```

Oczekiwane: FAIL — `404` zamiast `401/403` (URL nie zarejestrowany).

- [ ] **Krok 3: Utwórz `analysis/serializers.py`**

```python
from rest_framework import serializers

from .models import BookCharacter, CharacterRelationship


class BookCharacterSerializer(serializers.ModelSerializer):
    mention_count = serializers.IntegerField()
    is_hidden = serializers.BooleanField()

    class Meta:
        model = BookCharacter
        fields = ("id", "slug", "name", "description", "mention_count", "source", "confidence", "is_hidden")


class CharacterRelationSerializer(serializers.ModelSerializer):
    from_character_name = serializers.CharField(source="from_character.name", read_only=True)
    to_character_name = serializers.CharField(source="to_character.name", read_only=True)
    from_character_slug = serializers.CharField(source="from_character.slug", read_only=True)
    to_character_slug = serializers.CharField(source="to_character.slug", read_only=True)

    class Meta:
        model = CharacterRelationship
        fields = (
            "id",
            "from_character_name",
            "from_character_slug",
            "to_character_name",
            "to_character_slug",
            "relation_type",
        )


class AIExtractionSerializer(serializers.Serializer):
    """Read-only serializer for the AIExtraction payload (1-1 with Book)."""

    id = serializers.SerializerMethodField()
    book_id = serializers.IntegerField(source="pk")
    status = serializers.CharField(source="ai_extraction_status")
    created_at = serializers.DateTimeField(source="ai_extraction_started_at")
    finished_at = serializers.DateTimeField(source="ai_extraction_finished_at")
    failure_reason = serializers.SerializerMethodField()
    characters = serializers.SerializerMethodField()
    relations = serializers.SerializerMethodField()
    covered_through = serializers.SerializerMethodField()
    confidence_summary = serializers.SerializerMethodField()

    def get_id(self, obj):
        return str(obj.pk)

    def get_failure_reason(self, obj):
        return obj.ai_extraction_failure_reason or None

    def get_characters(self, obj):
        return BookCharacterSerializer(obj.characters.all(), many=True).data

    def get_relations(self, obj):
        return CharacterRelationSerializer(obj.character_relationships.all(), many=True).data

    def get_covered_through(self, obj):
        return {
            "chapter": None,
            "percentage": 100 if obj.ai_extraction_status == "done" else None,
        }

    def get_confidence_summary(self, obj):
        visible_ai_chars = obj.characters.filter(is_hidden=False, source__in=["ai", "ai-verified"])
        confidences = [c.confidence for c in visible_ai_chars if c.confidence is not None]
        if not confidences:
            return {"overall": None, "flagged_low": 0}
        avg = sum(confidences) / len(confidences)
        flagged = sum(1 for c in confidences if c < 0.5)
        return {"overall": round(avg, 3), "flagged_low": flagged}
```

- [ ] **Krok 4: Utwórz `analysis/views.py`**

```python
from django.shortcuts import get_object_or_404
from rest_framework import status
from rest_framework.permissions import IsAdminUser
from rest_framework.response import Response
from rest_framework.views import APIView

from books.models import Book

from .models import BookCharacter
from .serializers import AIExtractionSerializer, BookCharacterSerializer
from .tasks import analyse_book


class AIExtractionTriggerView(APIView):
    permission_classes = [IsAdminUser]

    def post(self, request, book_id):
        book = get_object_or_404(
            Book.objects.prefetch_related("characters", "character_relationships"),
            pk=book_id,
        )
        # Idempotent: do not dispatch if already in-flight
        if book.ai_extraction_status in ("pending", "running"):
            return Response(AIExtractionSerializer(book).data, status=status.HTTP_200_OK)

        Book.objects.filter(pk=book_id).update(
            ai_extraction_status="pending",
            ai_extraction_failure_reason="",
        )
        analyse_book.delay(book_id)

        book.refresh_from_db()
        return Response(AIExtractionSerializer(book).data, status=status.HTTP_202_ACCEPTED)


class AIExtractionRetrieveView(APIView):
    permission_classes = [IsAdminUser]

    def get(self, request, book_id):
        book = get_object_or_404(
            Book.objects.prefetch_related("characters", "character_relationships"),
            pk=book_id,
        )
        return Response(AIExtractionSerializer(book).data)


class BookCharacterHideView(APIView):
    permission_classes = [IsAdminUser]

    def post(self, request, book_id, character_id):
        char = get_object_or_404(BookCharacter, pk=character_id, book_id=book_id)
        hidden = bool(request.data.get("hidden", True))
        char.is_hidden = hidden
        char.save(update_fields=["is_hidden"])
        return Response(BookCharacterSerializer(char).data)
```

- [ ] **Krok 5: Utwórz `analysis/urls.py`**

```python
from django.urls import path

from .views import AIExtractionRetrieveView, AIExtractionTriggerView, BookCharacterHideView

urlpatterns = [
    path("books/<int:book_id>/ai/extract/", AIExtractionTriggerView.as_view()),
    path("books/<int:book_id>/ai/extraction/", AIExtractionRetrieveView.as_view()),
    path("books/<int:book_id>/characters/<int:character_id>/hide/", BookCharacterHideView.as_view()),
]
```

- [ ] **Krok 6: Dodaj include do `config/urls.py`**

W `backend-django/config/urls.py` dodaj po istniejących `path("api/books/", ...)`:

```python
    path("api/", include("analysis.urls")),
```

Finalny `config/urls.py`:

```python
from django.contrib import admin
from django.urls import include, path
from drf_spectacular.views import SpectacularAPIView, SpectacularSwaggerView

urlpatterns = [
    path("admin/", admin.site.urls),
    path("api/auth/", include("users.urls.auth")),
    path("api/users/", include("users.urls.users")),
    path("api/books/", include("books.urls")),
    path("api/shelf/", include("shelf.urls")),
    path("api/authors/", include("library.urls.authors")),
    path("api/series/", include("library.urls.series")),
    path("api/genres/", include("library.urls.genres")),
    path("api/reviews/", include("reviews.urls")),
    path("api/", include("analysis.urls")),  # ai extract, hide — scoped to /api/books/:id/
    path("api/schema/", SpectacularAPIView.as_view(), name="schema"),
    path(
        "api/docs/",
        SpectacularSwaggerView.as_view(url_name="schema"),
        name="swagger-ui",
    ),
]
```

**Uwaga o routingu:** Django próbuje wzorce w kolejności i backtrackuje z include jeśli wewnątrz nie ma dopasowania. `api/books/<id>/ai/extract/` trafi najpierw do `api/books/` include (books.urls nie ma wzorca `<id>/ai/extract/`), Django backtrackuje i próbuje `api/` include (analysis.urls) — gdzie `books/<int:book_id>/ai/extract/` pasuje. Kolejność ma znaczenie: `api/books/` musi być przed `api/`.

- [ ] **Krok 7: Uruchom testy widoków — green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test analysis.tests.test_views -v 2
```

Oczekiwane: wszystkie PASS (15+ testów).

- [ ] **Krok 8: Pełen suite**

```bash
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS.

- [ ] **Krok 9: Lint**

```bash
uv run ruff check .
```

Oczekiwane: 0 błędów.

- [ ] **Krok 10: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/analysis/serializers.py backend-django/analysis/views.py backend-django/analysis/urls.py backend-django/config/urls.py backend-django/analysis/tests/test_views.py
git commit -m "feat(analysis): AIExtraction endpoints + hide character endpoint"
```

---

## Task 5: `BookCharacterSerializer` update + `BookRetrieveView` admin/non-admin filtering

**Files:**
- Modify: `backend-django/books/serializers.py`
- Modify: `backend-django/books/views.py`

- [ ] **Krok 1: Zaktualizuj `BookCharacterSerializer` w `books/serializers.py:8-13`**

Zastąp istniejący `BookCharacterSerializer`:

```python
class BookCharacterSerializer(serializers.ModelSerializer):
    mention_count = serializers.IntegerField()

    class Meta:
        model = BookCharacter
        fields = ("id", "slug", "name", "description", "mention_count", "source", "confidence", "is_hidden")
```

**Uwaga:** `mentionCount` (camelCase) jest zastąpione przez `mention_count` (snake_case) — zgodnie z konwencją snake_case z `03-api-contract.md`. Vue (faza przejściowa 2.1-2.6) używał camelCase — to jest breaking change dla Vue na tym polu. Ponieważ Vue umiera w Phase 2.6, akceptowalne.

- [ ] **Krok 2: Zaktualizuj `BookDetailSerializer.to_representation` w `books/serializers.py:87-122`**

W metodzie `to_representation` zamień fragment serializacji charakterów:

```python
        is_admin = request and request.user and request.user.is_staff
        chars_qs = instance.characters.all() if is_admin else instance.characters.filter(is_hidden=False)
        characters = BookCharacterSerializer(chars_qs, many=True).data
```

Oraz zaktualizuj linię `book_data["analysisStatus"]` aby używała nowego pola:

```python
        book_data["analysisStatus"] = {
            "analysisFinished": instance.characters.filter(is_hidden=False).exists(),
            "status": instance.ai_extraction_status,
        }
```

Finalny blok `to_representation` (zastąp od linii `def to_representation` do końca):

```python
    def to_representation(self, instance):
        request = self.context.get("request")
        book_data = super().to_representation(instance)

        is_admin = bool(request and request.user and request.user.is_staff)

        book_data["analysisStatus"] = {
            "analysisFinished": instance.characters.filter(is_hidden=False).exists(),
            "status": instance.ai_extraction_status,
        }
        book_data["seriesName"] = instance.serie.name if instance.serie else None

        shelf_entry = None
        if request and request.user.is_authenticated:
            entries = getattr(instance, "current_user_shelf_entries", None)
            entry = entries[0] if entries else None
            if entry is not None:
                shelf_entry = {
                    "status": entry.status,
                    "createdAt": entry.created_at.isoformat(),
                }

        chars_qs = instance.characters.all() if is_admin else instance.characters.filter(is_hidden=False)
        characters = BookCharacterSerializer(chars_qs, many=True).data

        relations = CharacterRelationSerializer(
            instance.character_relationships.all(), many=True
        ).data

        return {
            "book": book_data,
            "shelfEntry": shelf_entry,
            "characters": characters,
            "relations": relations,
        }
```

- [ ] **Krok 3: Zaktualizuj `BookRetrieveView` w `books/views.py` — Prefetch z filtrem `is_hidden`**

W `BookRetrieveView.get_object()` zastąp prefetch `characters`:

```python
    def get_object(self):
        from analysis.models import BookCharacter

        id_or_slug = self.kwargs.get("id_or_slug") or str(self.kwargs.get("pk", ""))
        is_admin = self.request.user and self.request.user.is_staff

        chars_qs = (
            BookCharacter.objects.all()
            if is_admin
            else BookCharacter.objects.filter(is_hidden=False)
        )

        qs = Book.objects.select_related("serie").prefetch_related(
            Prefetch("characters", queryset=chars_qs),
            "tags",
            "genres",
            "authors",
            "character_relationships__from_character",
            "character_relationships__to_character",
        )
        if self.request.user.is_authenticated:
            qs = qs.prefetch_related(
                Prefetch(
                    "shelf_entries",
                    queryset=ShelfEntry.objects.filter(user=self.request.user),
                    to_attr="current_user_shelf_entries",
                )
            )
        book = resolve_book(id_or_slug)
        return qs.get(pk=book.pk)
```

- [ ] **Krok 4: Uruchom pełen suite**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS. Szczególnie ważne: `books.tests.*` nadal przechodzą.

- [ ] **Krok 5: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/books/serializers.py backend-django/books/views.py
git commit -m "feat(books): BookCharacterSerializer with new fields + is_hidden filter for non-admin"
```

---

## Task 6: `BookCharacterAdmin` — nowe pola + akcje

**Files:**
- Modify: `backend-django/analysis/admin.py`

- [ ] **Krok 1: Zaktualizuj `analysis/admin.py`**

Zastąp całość `backend-django/analysis/admin.py`:

```python
from django.contrib import admin, messages

from analysis.models import (
    BookCharacter,
    BookOrganization,
    BookPlace,
    CharacterRelationship,
)
from analysis.tasks import analyse_book


@admin.action(description="Mark selected characters as hidden")
def mark_hidden(modeladmin, request, queryset):
    updated = queryset.update(is_hidden=True)
    messages.success(request, f"{updated} character(s) marked as hidden.")


@admin.action(description="Mark selected characters as visible")
def mark_visible(modeladmin, request, queryset):
    updated = queryset.update(is_hidden=False)
    messages.success(request, f"{updated} character(s) marked as visible.")


@admin.register(BookCharacter)
class BookCharacterAdmin(admin.ModelAdmin):
    list_display = ("name", "slug", "book", "mention_count", "source", "is_hidden", "confidence")
    search_fields = ("name", "slug")
    list_filter = ("book", "source", "is_hidden")
    actions = [mark_hidden, mark_visible]
    readonly_fields = ("slug",)


@admin.register(BookPlace)
class BookPlaceAdmin(admin.ModelAdmin):
    list_display = ("name", "book", "mention_count")
    search_fields = ("name",)
    list_filter = ("book",)


@admin.register(BookOrganization)
class BookOrganizationAdmin(admin.ModelAdmin):
    list_display = ("name", "book", "mention_count")
    search_fields = ("name",)
    list_filter = ("book",)


@admin.register(CharacterRelationship)
class CharacterRelationshipAdmin(admin.ModelAdmin):
    list_display = ("from_character", "relation_type", "to_character", "book")
    list_filter = ("relation_type", "book")
    search_fields = ("from_character__name", "to_character__name")


@admin.action(description="Analyse selected books (NER + LLM)")
def analyze_selected_books(modeladmin, request, queryset):
    triggered = 0
    for book in queryset:
        if not book.text:
            continue
        analyse_book.delay(book.id)
        triggered += 1
    messages.success(request, f"Analysis dispatched for {triggered} books.")
```

- [ ] **Krok 2: Sprawdź że Django Admin bootuje**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py check
```

Oczekiwane: `System check identified no issues (0 silenced).`

- [ ] **Krok 3: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/analysis/admin.py
git commit -m "feat(analysis): BookCharacterAdmin with slug/source/is_hidden + mark_hidden/visible actions"
```

---

## Task 7: Regeneracja OpenAPI + akceptacja końcowa

**Files:**
- Modify: `docs/api/openapi.yml`

- [ ] **Krok 1: Pełen test suite finalny**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS.

- [ ] **Krok 2: Lint finalny**

```bash
uv run ruff check .
```

Oczekiwane: 0 błędów.

- [ ] **Krok 3: Regeneruj OpenAPI snapshot**

```bash
cd /home/dv6/GitHub/storyshelf
make regenerate-openapi
```

Oczekiwane: `docs/api/openapi.yml` zaktualizowany z nowymi endpointami i polami.

- [ ] **Krok 4: Sprawdź nowe endpointy w schemacie**

```bash
grep -n "ai/extract\|ai/extraction\|characters.*hide\|ai_extraction_status\|is_hidden\|confidence" docs/api/openapi.yml | head -20
```

Oczekiwane: wyniki pokazujące nowe ścieżki i pola.

- [ ] **Krok 5: Uruchom snapshot test**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test config.tests.test_openapi_schema -v 2
```

Oczekiwane: PASS.

- [ ] **Krok 6: Commit snapshot**

```bash
cd /home/dv6/GitHub/storyshelf
git add docs/api/openapi.yml
git commit -m "chore(api): regenerate OpenAPI snapshot after Phase 2.2 changes"
```

---

## Akceptacja całego planu

- [ ] `DJANGO_ENV=dev uv run python manage.py test` — wszystkie testy PASS
- [ ] `uv run ruff check .` — 0 błędów
- [ ] `POST /api/books/:id/ai/extract/` anon → 401; user → 403; admin → 202 + AIExtraction payload
- [ ] Drugi `POST` gdy `status==pending|running` → 200 (no-op, nie dispatch ponownie)
- [ ] `GET /api/books/:id/ai/extraction/` → payload z `status`, `characters[]`, `relations[]`, `covered_through`, `confidence_summary`, `failure_reason`
- [ ] `POST /api/books/:id/characters/:cid/hide/` z `{"hidden": true}` → `is_hidden=True`; z `{"hidden": false}` → cofa
- [ ] Re-run `analyse_book`: `BookCharacter(is_hidden=True, source="ai-verified")` przeżywa re-run bez zmiany tych pól
- [ ] `BookDetailSerializer` dla anon filtruje `is_hidden=True`; admin widzi wszystkie
- [ ] `slug` na `BookCharacter` unikalny per book; kolizja → sufix `-2`
- [ ] `docs/api/openapi.yml` zawiera 3 nowe endpointy + pola `slug`/`source`/`confidence`/`is_hidden` na Character
