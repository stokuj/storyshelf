# Phase 2.0 — Foundation for Svelte Migration — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zbudować fundament pod migrację Vue → Svelte: kontrakt API jako test wykonywalny (OpenAPI snapshot), cross-origin JWT cookies + CORS gotowe na osobne origin frontu, oraz pisemna polityka usunięcia Vue (ADR-004).

**Architecture:** Trzy niezależne, ale komplementarne obszary. (1) Snapshot test porównuje schemat wygenerowany in-process przez `SpectacularAPIView` z plikiem `docs/api/openapi.yml` zacommittowanym w repo. (2) Cookies JWT przechodzą z hardkodowanego `samesite="Lax"` na env-driven setting `JWT_COOKIE_SAMESITE`/`JWT_COOKIE_SECURE` z różnymi defaultami w dev (Lax, insecure) i prod (None, Secure). `CORS_ALLOW_CREDENTIALS=True` dodajemy w `base.py`. (3) ADR-004 spisuje plan usunięcia Vue jako check-listę do wykonania w Phase 3.0 — Phase 2.0 niczego nie kasuje.

**Tech Stack:** Django 6, DRF 3.17, `drf-spectacular>=0.29`, `django-cors-headers>=4.9` (już w deps), `djangorestframework-simplejwt>=5.5`, pytest-django, ruff, uv, GitHub Actions, Makefile.

---

## Pliki do zmiany

| Plik | Akcja |
|---|---|
| `backend-django/config/tests/__init__.py` | NOWY — pakiet testów config |
| `backend-django/config/tests/test_openapi_schema.py` | NOWY — snapshot test schematu |
| `docs/api/openapi.yml` | NOWY — wygenerowany schemat zacommittowany |
| `docs/api/README.md` | NOWY — how-to regeneracji + plan konsumpcji |
| `infra/scripts/regenerate-openapi.sh` | NOWY — utility script |
| `Makefile` | dodać cel `regenerate-openapi`, rozszerzyć `verify` o snapshot |
| `backend-django/config/settings/base.py` | dodać `CORS_ALLOW_CREDENTIALS=True`, defaults `JWT_COOKIE_*` |
| `backend-django/config/settings/dev.py` | jawny `JWT_COOKIE_SAMESITE="Lax"`, `JWT_COOKIE_SECURE=False`, opcjonalnie zacieśnić CORS |
| `backend-django/config/settings/prod.py` | env-driven `JWT_COOKIE_SAMESITE` (default `"None"`), `JWT_COOKIE_SECURE=True`, `JWT_COOKIE_DOMAIN` |
| `backend-django/users/cookie_auth.py` | refactor: czytaj `samesite`/`secure`/`domain` z Django settings |
| `backend-django/users/tests/test_cookie_auth.py` | update — przejść z hardkodowanej asercji `"Lax"` na sprawdzenie ze settings |
| `backend-django/users/tests/test_cors_jwt.py` | NOWY — preflight OPTIONS + cookie flags w dev vs prod |
| `docs/decisions/ADR-004-vue-removal.md` | NOWY — polityka + checklista Phase 3.0 |
| `.github/workflows/ci.yml` | dodać step który uruchamia snapshot test OpenAPI (`make verify` lub równoważny) |

---

## Out of scope (dla Phase 2.0)

- Paginacja, slug-based URLs, nowe endpointy, nowe modele (Shelf, Character cross-book) — Phase 2.1+
- Setup repo SvelteKit — Phase 3.0
- Generator typów TS (`openapi-typescript`) — Phase 3.0
- Faktyczne usunięcie Vue (ADR-004 to spisuje, ale wykonanie = Phase 3.0)
- Konfiguracja prod CORS dla konkretnych domen (zależy od decyzji deployu — roadmap pkt 1)
- HTTPS w dev (mkcert) — `Secure` cookies zostają wyłączone w dev settings

---

## Task 1: Snapshot test OpenAPI — utworzenie testu

**Files:**
- Create: `backend-django/config/tests/__init__.py`
- Create: `backend-django/config/tests/test_openapi_schema.py`

- [ ] **Krok 1: Utwórz pakiet testów config**

```bash
touch /home/dv6/GitHub/storyshelf/backend-django/config/tests/__init__.py
```

- [ ] **Krok 2: Napisz test snapshot (musi failować bo brakuje pliku `openapi.yml`)**

Utwórz `backend-django/config/tests/test_openapi_schema.py`:

```python
"""Snapshot test kontraktu API.

Test generuje schemat OpenAPI in-process przez drf-spectacular i porownuje
z plikiem `docs/api/openapi.yml` zacommittowanym w repo. PR ktory zmienia
API musi rownoczesnie zaktualizowac snapshot, inaczej CI faila.

Regeneracja po zmianie API:
    make regenerate-openapi
"""
from __future__ import annotations

from pathlib import Path

from django.test import TestCase
from django.urls import reverse
from rest_framework.test import APIClient


REPO_ROOT = Path(__file__).resolve().parent.parent.parent.parent
SNAPSHOT_PATH = REPO_ROOT / "docs" / "api" / "openapi.yml"


class OpenAPISchemaSnapshotTest(TestCase):
    """Wykrywa nieintencjonalne zmiany kontraktu API."""

    def test_generated_schema_matches_snapshot(self):
        self.assertTrue(
            SNAPSHOT_PATH.exists(),
            f"Brak snapshotu OpenAPI w {SNAPSHOT_PATH}. "
            f"Wygeneruj go: `make regenerate-openapi`.",
        )

        client = APIClient()
        url = reverse("schema")
        response = client.get(url, HTTP_ACCEPT="application/vnd.oai.openapi")
        self.assertEqual(response.status_code, 200, response.content[:500])

        generated = response.content.decode("utf-8")
        snapshot = SNAPSHOT_PATH.read_text(encoding="utf-8")

        if generated.strip() != snapshot.strip():
            self.fail(
                "Wygenerowany schemat OpenAPI rozni sie od snapshotu "
                f"({SNAPSHOT_PATH}).\n"
                "Jezeli zmiana jest zamierzona, zregeneruj snapshot:\n"
                "    make regenerate-openapi\n"
                "i zacommittuj rownoczesnie z PR-em zmieniajacym API."
            )
```

- [ ] **Krok 3: Uruchom test — upewnij się że failuje (brak `openapi.yml`)**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test config.tests.test_openapi_schema -v 2
```

Oczekiwane: FAIL — `AssertionError: Brak snapshotu OpenAPI w .../docs/api/openapi.yml`.

- [ ] **Krok 4: Commit testu (red)**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/config/tests/__init__.py backend-django/config/tests/test_openapi_schema.py
git commit -m "test(config): add OpenAPI schema snapshot test (red)"
```

---

## Task 2: Snapshot test OpenAPI — utility script + Makefile + snapshot

**Files:**
- Create: `infra/scripts/regenerate-openapi.sh`
- Modify: `Makefile`
- Create: `docs/api/openapi.yml`
- Create: `docs/api/README.md`

- [ ] **Krok 1: Utwórz skrypt regeneracji**

`infra/scripts/regenerate-openapi.sh`:

```bash
#!/usr/bin/env bash
# Regeneruje snapshot OpenAPI dla testu kontraktu.
#
# Wywolanie:
#     make regenerate-openapi
# lub bezposrednio:
#     ./infra/scripts/regenerate-openapi.sh
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
OUT_FILE="${REPO_ROOT}/docs/api/openapi.yml"

mkdir -p "$(dirname "${OUT_FILE}")"
cd "${REPO_ROOT}/backend-django"
DJANGO_ENV=dev uv run python manage.py spectacular \
    --file "${OUT_FILE}" \
    --format openapi

echo "OpenAPI snapshot updated: ${OUT_FILE}"
```

Nadaj prawa wykonywania:

```bash
chmod +x /home/dv6/GitHub/storyshelf/infra/scripts/regenerate-openapi.sh
```

- [ ] **Krok 2: Dodaj cel `regenerate-openapi` do `Makefile`**

Zmień `.PHONY` (linia 1) dodając `regenerate-openapi`:

```makefile
.PHONY: dev-up dev-down dev-status dev-build dev-superuser prod-up prod-down prod-status prod-logs verify regenerate-openapi
```

Dodaj nowy cel na końcu pliku (po `verify`):

```makefile
regenerate-openapi:
	$(ROOT_DIR)infra/scripts/regenerate-openapi.sh
```

- [ ] **Krok 3: Wygeneruj snapshot OpenAPI**

```bash
cd /home/dv6/GitHub/storyshelf
make regenerate-openapi
```

Oczekiwane: powstaje `docs/api/openapi.yml` (kilkaset linii), exit 0.

- [ ] **Krok 4: Utwórz README sekcji API**

`docs/api/README.md`:

```markdown
# API Contract

Snapshot OpenAPI 3.x dla backendu StoryShelf — wykonywalny kontrakt API.

## Pliki

- `openapi.yml` — wygenerowany schemat (drf-spectacular). Snapshotowany do repo, weryfikowany testem `config.tests.test_openapi_schema`.

## Regeneracja

Po kazdej zmianie API (nowy endpoint, zmiana serializera, nowe pole):

    make regenerate-openapi

Zacommittuj zmieniony `docs/api/openapi.yml` w tym samym PR-ze co zmiana backendu. Jezeli zapomnisz — CI faila na `config.tests.test_openapi_schema.OpenAPISchemaSnapshotTest`.

## Konsumpcja (Phase 3.0+)

Frontend Svelte generuje typy TS z tego pliku przez `openapi-typescript` (do dodania w Phase 3.0). W Phase 2.0 plik istnieje tylko jako test kontraktu — bezposrednich konsumentow w runtime brak.
```

- [ ] **Krok 5: Uruchom test snapshot — green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test config.tests.test_openapi_schema -v 2
```

Oczekiwane: PASS — `test_generated_schema_matches_snapshot ... ok`.

- [ ] **Krok 6: Sanity check — pełen test suite**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie testy PASS (nic nie zostało zepsute).

- [ ] **Krok 7: Commit (green)**

```bash
cd /home/dv6/GitHub/storyshelf
git add infra/scripts/regenerate-openapi.sh Makefile docs/api/openapi.yml docs/api/README.md
git commit -m "feat(config): add OpenAPI snapshot + regeneration script + Makefile target"
```

---

## Task 3: Cross-origin cookies — settings (defaults + env override)

**Files:**
- Modify: `backend-django/config/settings/base.py`
- Modify: `backend-django/config/settings/dev.py`
- Modify: `backend-django/config/settings/prod.py`

- [ ] **Krok 1: Dodaj domyślne wartości w `base.py`**

W `backend-django/config/settings/base.py` zamień blok od linii 134 (obecne `CORS_ALLOW_ALL_ORIGINS = False`) na:

```python
# --- CORS (django-cors-headers) ---
# Cross-origin policy: explicit allowlist + credentials (wymagane dla cookies JWT).
# Konkretne origins ustawia kazde srodowisko (dev.py / prod.py).
CORS_ALLOW_ALL_ORIGINS = False
CORS_ALLOW_CREDENTIALS = True

# --- JWT cookies (uzywane przez users.cookie_auth.set_jwt_cookies) ---
# Defaulty bezpieczne dla prod; dev nadpisuje na luzniejsze (Lax, insecure).
JWT_COOKIE_SAMESITE = os.getenv("JWT_COOKIE_SAMESITE", "Lax")
JWT_COOKIE_SECURE = os.getenv("JWT_COOKIE_SECURE", "false").lower() == "true"
JWT_COOKIE_DOMAIN = os.getenv("JWT_COOKIE_DOMAIN") or None
```

- [ ] **Krok 2: Dopisz jawnie wartości dev w `dev.py`**

W `backend-django/config/settings/dev.py` po linii `CORS_ALLOW_ALL_ORIGINS = True` (linia 11) dodaj:

```python
# JWT cookies dev: same-origin (Vue 5173 + API 8000), brak HTTPS lokalnie
JWT_COOKIE_SAMESITE = "Lax"
JWT_COOKIE_SECURE = False
JWT_COOKIE_DOMAIN = None
```

**Decyzja CORS dev:** zostawiamy `CORS_ALLOW_ALL_ORIGINS = True` jak dotychczas — w Phase 3.0 (gdy Svelte 5174 i Vue 5173 chwilę żyją równolegle) i tak nie chcemy ręcznie zarządzać listą; ryzyko produkcyjne zerowe, bo prod ma explicit allowlist.

- [ ] **Krok 3: Dopisz wartości prod w `prod.py`**

W `backend-django/config/settings/prod.py` po istniejącym bloku CORS (linia 26-27) dodaj:

```python
# JWT cookies prod: cross-origin (Svelte na osobnej domenie), wymaga HTTPS
JWT_COOKIE_SAMESITE = os.getenv("JWT_COOKIE_SAMESITE", "None")
JWT_COOKIE_SECURE = os.getenv("JWT_COOKIE_SECURE", "true").lower() == "true"
JWT_COOKIE_DOMAIN = os.getenv("JWT_COOKIE_DOMAIN") or None

# SameSite=None bez Secure jest odrzucane przez przegladarki. Fail fast.
if JWT_COOKIE_SAMESITE == "None" and not JWT_COOKIE_SECURE:
    raise ValueError(
        "JWT_COOKIE_SECURE must be true when JWT_COOKIE_SAMESITE=None (browsers reject otherwise)"
    )
```

- [ ] **Krok 4: Sprawdź że Django bootuje w obu trybach**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py check
DJANGO_ENV=prod DJANGO_SECRET_KEY=test ALLOWED_HOSTS=example.com CSRF_TRUSTED_ORIGINS=https://example.com uv run python manage.py check
```

Oczekiwane: `System check identified no issues (0 silenced).` w obu wywołaniach.

- [ ] **Krok 5: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/config/settings/base.py backend-django/config/settings/dev.py backend-django/config/settings/prod.py
git commit -m "feat(settings): env-driven JWT cookie flags + CORS_ALLOW_CREDENTIALS"
```

---

## Task 4: Refactor `cookie_auth.py` na settings-driven

**Files:**
- Modify: `backend-django/users/cookie_auth.py`
- Modify: `backend-django/users/tests/test_cookie_auth.py`

- [ ] **Krok 1: Zaktualizuj istniejące testy — przygotuj asercje na settings**

W `backend-django/users/tests/test_cookie_auth.py` zamień klasę `SetJWTCookiesTest` (linie 57-84) na:

```python
class SetJWTCookiesTest(TestCase):
    def test_sets_access_cookie_with_settings_samesite(self):
        response = MagicMock()
        set_jwt_cookies(response, "access_token_value")
        response.set_cookie.assert_called_once()
        args, kwargs = response.set_cookie.call_args
        self.assertEqual(args[0], ACCESS_COOKIE)
        self.assertEqual(args[1], "access_token_value")
        self.assertTrue(kwargs["httponly"])
        # Dev settings: SameSite=Lax, Secure=False, no domain
        self.assertEqual(kwargs["samesite"], "Lax")
        self.assertFalse(kwargs["secure"])

    def test_sets_both_cookies_when_refresh_provided(self):
        response = MagicMock()
        set_jwt_cookies(response, "access_value", "refresh_value")
        self.assertEqual(response.set_cookie.call_count, 2)
        calls = [c.args[0] for c in response.set_cookie.call_args_list]
        self.assertIn(ACCESS_COOKIE, calls)
        self.assertIn(REFRESH_COOKIE, calls)

    def test_refresh_cookie_has_correct_path(self):
        response = MagicMock()
        set_jwt_cookies(response, "access_value", "refresh_value")
        for call in response.set_cookie.call_args_list:
            if call.args[0] == REFRESH_COOKIE:
                self.assertEqual(call.kwargs["path"], REFRESH_COOKIE_PATH)
                break
        else:
            self.fail("Refresh cookie not set")

    def test_respects_overridden_samesite_and_secure(self):
        from django.test import override_settings

        response = MagicMock()
        with override_settings(JWT_COOKIE_SAMESITE="None", JWT_COOKIE_SECURE=True):
            set_jwt_cookies(response, "access_value", "refresh_value")
        for call in response.set_cookie.call_args_list:
            self.assertEqual(call.kwargs["samesite"], "None")
            self.assertTrue(call.kwargs["secure"])

    def test_passes_cookie_domain_when_set(self):
        from django.test import override_settings

        response = MagicMock()
        with override_settings(JWT_COOKIE_DOMAIN=".storyshelf.example.com"):
            set_jwt_cookies(response, "access_value", "refresh_value")
        for call in response.set_cookie.call_args_list:
            self.assertEqual(call.kwargs["domain"], ".storyshelf.example.com")
```

- [ ] **Krok 2: Uruchom testy — `test_respects_overridden_*` i `test_passes_cookie_domain_*` powinny failować**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test users.tests.test_cookie_auth.SetJWTCookiesTest -v 2
```

Oczekiwane: część FAIL (cookie_auth nadal hardkoduje "Lax", nie czyta settings, nie przekazuje `domain`).

- [ ] **Krok 3: Zrefaktoryzuj `users/cookie_auth.py`**

Zamień całość `backend-django/users/cookie_auth.py` na:

```python
from django.conf import settings
from rest_framework_simplejwt.authentication import JWTAuthentication
from rest_framework_simplejwt.exceptions import InvalidToken

ACCESS_COOKIE = "access_token"
REFRESH_COOKIE = "refresh_token"
REFRESH_COOKIE_PATH = "/api/auth/refresh/"


class JWTCookieAuthentication(JWTAuthentication):
    """Reads JWT from HttpOnly cookie instead of Authorization header."""

    def authenticate(self, request):
        raw_token = request.COOKIES.get(ACCESS_COOKIE)
        if raw_token is None:
            return None
        try:
            validated_token = self.get_validated_token(raw_token)
        except InvalidToken:
            return None
        return self.get_user(validated_token), validated_token


def _cookie_flags() -> dict:
    """Pull cross-origin cookie flags from Django settings (env-driven, see settings/base.py)."""
    flags = {
        "samesite": getattr(settings, "JWT_COOKIE_SAMESITE", "Lax"),
        "secure": getattr(settings, "JWT_COOKIE_SECURE", False),
    }
    domain = getattr(settings, "JWT_COOKIE_DOMAIN", None)
    if domain:
        flags["domain"] = domain
    return flags


def set_jwt_cookies(response, access_token, refresh_token=None):
    """Set HttpOnly JWT cookies on response. access/refresh can be token objects or strings."""
    import os

    access_max_age = int(os.getenv("JWT_ACCESS_LIFETIME_MINUTES", "30")) * 60
    refresh_max_age = int(os.getenv("JWT_REFRESH_LIFETIME_MINUTES", "1440")) * 60
    flags = _cookie_flags()

    response.set_cookie(
        ACCESS_COOKIE,
        str(access_token),
        httponly=True,
        max_age=access_max_age,
        **flags,
    )
    if refresh_token is not None:
        response.set_cookie(
            REFRESH_COOKIE,
            str(refresh_token),
            httponly=True,
            max_age=refresh_max_age,
            path=REFRESH_COOKIE_PATH,
            **flags,
        )


def clear_jwt_cookies(response):
    """Expire both JWT cookies."""
    response.delete_cookie(ACCESS_COOKIE)
    response.delete_cookie(REFRESH_COOKIE, path=REFRESH_COOKIE_PATH)
```

**Uwaga:** `_cookie_flags()` warunkowo dodaje `domain` tylko gdy ustawione — inaczej w dev przekazalibyśmy `domain=None` co zmienia semantykę `response.set_cookie` w niektórych asercjach.

- [ ] **Krok 4: Uruchom testy cookie_auth — wszystkie green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test users.tests.test_cookie_auth -v 2
```

Oczekiwane: wszystkie PASS (5+ testów w `SetJWTCookiesTest`).

- [ ] **Krok 5: Pełen test suite — sanity check**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS (nic innego się nie zepsuło, w szczególności `users.tests.test_views` i `test_openapi_schema`).

- [ ] **Krok 6: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/users/cookie_auth.py backend-django/users/tests/test_cookie_auth.py
git commit -m "refactor(users): drive JWT cookie samesite/secure/domain from settings"
```

---

## Task 5: Test CORS + JWT cookies (preflight + flagi prod vs dev)

**Files:**
- Create: `backend-django/users/tests/test_cors_jwt.py`

- [ ] **Krok 1: Napisz test integracyjny**

Utwórz `backend-django/users/tests/test_cors_jwt.py`:

```python
"""CORS preflight + cookie flags w dev vs prod settings.

Nie testujemy prod settings przez DJANGO_ENV=prod (wymagaloby resetu modulu),
zamiast tego uzywamy override_settings aby zsymulowac cross-origin profile.
"""
from __future__ import annotations

from django.test import TestCase, override_settings
from rest_framework import status
from rest_framework.test import APIClient

from users.models import User


class CorsPreflightTest(TestCase):
    """Preflight OPTIONS z Origin headerem zwraca Access-Control-Allow-* z credentials."""

    def setUp(self):
        self.client = APIClient()

    def test_options_preflight_returns_cors_headers(self):
        response = self.client.options(
            "/api/auth/login/",
            HTTP_ORIGIN="http://localhost:5174",
            HTTP_ACCESS_CONTROL_REQUEST_METHOD="POST",
            HTTP_ACCESS_CONTROL_REQUEST_HEADERS="content-type",
        )
        # corsheaders middleware odpowiada 200 (a nie 405) na preflight
        self.assertIn(response.status_code, (status.HTTP_200_OK, status.HTTP_204_NO_CONTENT))
        self.assertEqual(
            response.get("Access-Control-Allow-Origin"), "http://localhost:5174"
        )
        self.assertEqual(response.get("Access-Control-Allow-Credentials"), "true")


class LoginCookieFlagsDevTest(TestCase):
    """Login w dev settings: Set-Cookie bez Secure, SameSite=Lax."""

    def setUp(self):
        self.client = APIClient()
        self.user = User.objects.create_user(
            email="cors@test.com", username="corsuser", password="secret123"
        )

    def test_dev_login_sets_lax_insecure_cookie(self):
        response = self.client.post(
            "/api/auth/login/",
            {"username": "corsuser", "password": "secret123"},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_200_OK, response.content[:300])
        set_cookie_headers = response.cookies
        self.assertIn("access_token", set_cookie_headers)
        access = set_cookie_headers["access_token"]
        # SameSite=Lax w dev
        self.assertEqual(access["samesite"], "Lax")
        # Secure puste/false w dev (http://localhost)
        self.assertFalse(access["secure"])


@override_settings(JWT_COOKIE_SAMESITE="None", JWT_COOKIE_SECURE=True)
class LoginCookieFlagsProdProfileTest(TestCase):
    """Symulacja prod settings: Set-Cookie z Secure i SameSite=None."""

    def setUp(self):
        self.client = APIClient()
        self.user = User.objects.create_user(
            email="cors2@test.com", username="corsuser2", password="secret123"
        )

    def test_prod_profile_login_sets_none_secure_cookie(self):
        response = self.client.post(
            "/api/auth/login/",
            {"username": "corsuser2", "password": "secret123"},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_200_OK, response.content[:300])
        access = response.cookies["access_token"]
        self.assertEqual(access["samesite"], "None")
        self.assertTrue(access["secure"])
```

**Uwaga o ścieżkach:** test zakłada że endpoint loginu to `/api/auth/login/` (zgodne z `ACCESS_COOKIE` path `/api/auth/refresh/` w `cookie_auth.py`). Jeśli rzeczywista ścieżka jest inna (np. `/api/users/token/`), zmień URL — sprawdź `backend-django/config/urls.py` lub `users/urls.py`.

- [ ] **Krok 2: Uruchom test — powinien przejść w całości**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test users.tests.test_cors_jwt -v 2
```

Oczekiwane: 3 PASS. Jeśli `CorsPreflightTest` faila — najpewniej dev używa `CORS_ALLOW_ALL_ORIGINS=True` i middleware nie ustawia `Access-Control-Allow-Credentials` dla wildcard origin. Wtedy w `dev.py` zamień:

```python
CORS_ALLOW_ALL_ORIGINS = True
```

na:

```python
CORS_ALLOW_ALL_ORIGINS = False
CORS_ALLOWED_ORIGINS = [
    "http://localhost:5173",  # Vue (transitional)
    "http://localhost:5174",  # Svelte (Phase 3.0)
    "http://127.0.0.1:5173",
    "http://127.0.0.1:5174",
]
```

i ponów test.

- [ ] **Krok 3: Pełen test suite + lint**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
uv run ruff check .
```

Oczekiwane: wszystkie PASS, ruff exit 0.

- [ ] **Krok 4: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/users/tests/test_cors_jwt.py
# Jezeli zmienilas dev.py CORS:
git add backend-django/config/settings/dev.py
git commit -m "test(users): CORS preflight + JWT cookie flags dev vs prod profile"
```

---

## Task 6: ADR-004 Vue removal policy

**Files:**
- Create: `docs/decisions/ADR-004-vue-removal.md`

- [ ] **Krok 1: Sprawdź konwencję istniejących ADR**

Odczytaj `docs/decisions/ADR-001-jwt-httponly-cookies.md` i `ADR-002-dwa-workery-celery.md` aby trzymać identyczny układ sekcji (Kontekst, Opcje rozważone, Decyzja, Konsekwencje, Linki).

- [ ] **Krok 2: Utwórz ADR-004**

`docs/decisions/ADR-004-vue-removal.md`:

```markdown
# ADR-004: Usunięcie Vue 3 SPA jako pierwsza akcja Phase 3.0

**Status:** Zaakceptowane
**Data:** 2026-05-23
**Supersedes:** brak

## Kontekst

Phase 3.0 (migracja na SvelteKit) wymaga decyzji co zrobic z istniejacym Vue frontendem. Trzy opcje rozwazane podczas brainstormingu Phase 2.0 (2026-05-23):

1. **Okres przejsciowy** — Vue + Svelte zyja rownolegle, Caddy routuje po sciezce / nagłowku, uzytkownik dostaje stary albo nowy UI w zaleznosci od feature flag.
2. **Soft remove** — Vue zostaje w repo jako `frontend-legacy/`, ale nie jest budowane ani serwowane.
3. **Hard remove** — `rm -rf frontend/`, usuniecie serwisu Docker, czysciec konfiguracji w jednym commicie.

Vue jest traktowany jako **tymczasowy** od dnia podjecia decyzji o migracji na Svelte (roadmap 2026-05-22). Brak konsumentow zewnetrznych, brak SLA, brak okresu wsparcia. Trzymanie kodu ktory nigdy nie wroci to dlug techniczny i ryzyko nieintencjonalnego deployu starego UI.

## Opcje rozważone

| Opcja | Plus | Minus |
|---|---|---|
| Okres przejsciowy | Mozliwosc A/B, rollback | Caddy routing-kazoo, podwojny build CI, podwojny powierzchni bug |
| Soft remove | Latwy rollback, historia git zachowana | "Martwy" kod w repo myli przyszle agenty/devow; trzeba pamietac zeby go nie modyfikowac |
| Hard remove | Czystosc, jasna intencja | Brak rollback bez `git revert` (akceptowalne — historia git zostaje) |

## Decyzja

**Hard remove w jednym commicie** jako **pierwsza akcja Phase 3.0** (przed `mkdir frontend-svelte/`). Phase 2.0 niczego nie kasuje — Vue zostaje w pelni funkcjonalny do konca Phase 2. Phase 2.0 jedynie przygotowuje fundament (CORS, snapshot API) zeby Phase 3.0 mogla zaczac od `rm -rf frontend/`.

### Checklista do wykonania w Phase 3.0 (commit `chore: remove vue frontend`)

- [ ] `rm -rf frontend/` — cala Vue 3 SPA (`src/`, `tests/`, `vite.config.js`, `package.json`, `Dockerfile.prod`, `Dockerfile.dev`)
- [ ] `infra/compose/docker-compose.dev.yml` — usun serwis `frontend` / `vue`
- [ ] `infra/compose/docker-compose.prod.yml` — usun serwis `frontend` / `vue`
- [ ] `infra/caddy/Caddyfile` (lub odpowiednik) — root `/` → vue zamien na placeholder (`respond "Migration in progress"`) lub od razu skieruj na nowy build Svelte jezeli juz gotowy
- [ ] `Makefile` — sprawdz cele `dev-up` (output mentions `localhost:5173`); zaktualizuj/usun
- [ ] `.github/workflows/ci.yml` — usun step `build-and-push` dla `ghcr.io/stokuj/storyshelf-frontend:main` (linie 69-74 wg stanu z Phase 2.0)
- [ ] `CLAUDE.md` — sekcja `## Komendy → Frontend (z frontend/)` — zaktualizuj na komendy Svelte (lub `[do dodania w Phase 3.x]`)
- [ ] `docs/ARCHITECTURE.md` — tabela Tech Stack: `Vue 3 + Vite` → `SvelteKit`; sekcja "Przepływ danych" przemianowac `Vue 3 (Nginx)` na `Svelte (Nginx)`
- [ ] `docs/ROADMAP.md` — przeniesc pkt 5 "Migracja frontendu Vue 3 → Svelte" do "Zrobione"
- [ ] Backend bez zmian — CORS juz przygotowane w Phase 2.0 na cross-origin Svelte

## Konsekwencje

- **Brak rollback bez `git revert`** — akceptowalne, historia git jest pelna i tagujemy commit `pre-vue-removal` przed wykonaniem
- **Brak okresu przejsciowego** — w momencie deployu Phase 3.0 uzytkownik dostaje albo stary Vue (przed deployem), albo nowy Svelte (po). Akceptowalne dla hobby projektu bez SLA
- **JWT cookies juz cross-origin gotowe** (Phase 2.0, ADR-001 + ten ADR): Svelte hostowany na osobnym origin nie wymaga zmian w backendzie
- **API kontrakt jako test** (Phase 2.0): Svelte konsumuje `docs/api/openapi.yml` przez `openapi-typescript` — rozjazd backend ↔ frontend wykrywany w CI, nie w runtime

## Linki

- Spec: `docs/superpowers/specs/2026-05-23-phase2.0-foundation.md` (Obszar 3)
- Plan: `docs/superpowers/plans/2026-05-23-phase2.0-foundation.md` (Task 6)
- Powiazany ADR: [ADR-001](ADR-001-jwt-httponly-cookies.md) — JWT cookies (zostaja, Svelte ich uzywa)
- Roadmapa: `docs/ROADMAP.md` pkt 5 "Następne"
```

- [ ] **Krok 3: Sanity check — link w roadmapie/specu się zgadza**

```bash
grep -n "ADR-004" /home/dv6/GitHub/storyshelf/docs/superpowers/specs/2026-05-23-phase2.0-foundation.md
grep -n "ADR-004" /home/dv6/GitHub/storyshelf/docs/ROADMAP.md
```

Jeśli istnieją odwołania do `ADR-004` ze starszych dokumentów — zweryfikuj że ścieżka pasuje (`docs/decisions/ADR-004-vue-removal.md`).

- [ ] **Krok 4: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add docs/decisions/ADR-004-vue-removal.md
git commit -m "docs(adr): ADR-004 hard-remove Vue jako pierwsza akcja Phase 3.0"
```

---

## Task 7: CI integration — wywołaj snapshot test w workflow

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `Makefile` (rozszerzenie `verify`)

- [ ] **Krok 1: Rozszerz `make verify` o jawne wywołanie snapshot testu**

W `Makefile` zamień cel `verify` (linie 41-44) na:

```makefile
verify:
	cd $(ROOT_DIR)backend-django && uv run ruff check .
	cd $(ROOT_DIR)backend-django && DJANGO_ENV=dev uv run python manage.py check
	cd $(ROOT_DIR)backend-django && DJANGO_ENV=dev uv run python -m pytest
	cd $(ROOT_DIR)backend-django && DJANGO_ENV=dev uv run python manage.py test config.tests.test_openapi_schema --noinput
```

**Uwaga:** `pytest` powyzej rozkrywa wszystkie testy (w tym `unittest.TestCase`), wiec snapshot test prawdopodobnie jest juz uruchamiany. Powtorzenie przez `manage.py test ...` jest celowe — daje czytelny error message gdy CI faila, latwy do zlokalizowania w logu.

- [ ] **Krok 2: Dodaj step do workflow CI**

W `.github/workflows/ci.yml` w jobie `test` (po `Run Django tests`, linia 47-49) dodaj nowy step:

```yaml
      - name: Verify OpenAPI snapshot
        working-directory: backend-django
        run: DJANGO_ENV=dev uv run python manage.py test config.tests.test_openapi_schema --noinput
```

Caly job `test` wyglada po zmianie:

```yaml
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: booksdb
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: secret-key
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DJANGO_ENV: dev
      DATABASE_URL: postgres://postgres:secret-key@localhost:5432/booksdb
      DJANGO_SECRET_KEY: test-secret-key-for-ci
      OPENROUTER_API_KEY: sk-dummy
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.13"
      - name: Run Django tests
        working-directory: backend-django
        run: uv run python manage.py test --noinput
      - name: Verify OpenAPI snapshot
        working-directory: backend-django
        run: DJANGO_ENV=dev uv run python manage.py test config.tests.test_openapi_schema --noinput
```

**Dlaczego osobny step a nie `make verify`:** workflow ma juz `lint` job z `ruff`, a `make verify` powtarzalby lint w jobie `test`. Wydzielenie osobnego step daje czytelna nazwe w GitHub Actions UI gdy CI faila na rozjezdzonym kontrakcie.

- [ ] **Krok 3: Lokalna weryfikacja całości**

```bash
cd /home/dv6/GitHub/storyshelf
make verify
```

Oczekiwane: 4 kroki (ruff, check, pytest, test snapshot) wszystkie PASS, exit 0.

- [ ] **Krok 4: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add Makefile .github/workflows/ci.yml
git commit -m "ci: verify OpenAPI snapshot in test job + extend make verify"
```

---

## Akceptacja całego planu

Wykonaj te kroki ręcznie po Task 7 zanim otworzysz PR.

- [ ] **Wszystkie testy zielone**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
```

- [ ] **`make verify` przechodzi**

```bash
cd /home/dv6/GitHub/storyshelf
make verify
```

- [ ] **`docs/api/openapi.yml` zacommittowany i niepusty**

```bash
wc -l /home/dv6/GitHub/storyshelf/docs/api/openapi.yml
# Oczekiwane: >100 linii
git -C /home/dv6/GitHub/storyshelf log --oneline -- docs/api/openapi.yml
# Oczekiwane: co najmniej 1 commit
```

- [ ] **Preflight CORS test PASS**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test users.tests.test_cors_jwt -v 2
```

- [ ] **Login dev: cookie bez `Secure`; login prod profile: cookie `Secure` + `SameSite=None`** — pokryte testami `LoginCookieFlagsDevTest` i `LoginCookieFlagsProdProfileTest` (Task 5)

- [ ] **`ADR-004-vue-removal.md` istnieje z checklistą Phase 3.0**

```bash
ls /home/dv6/GitHub/storyshelf/docs/decisions/ADR-004-vue-removal.md
grep -c "^- \[ \]" /home/dv6/GitHub/storyshelf/docs/decisions/ADR-004-vue-removal.md
# Oczekiwane: >=8 checkbox items
```

- [ ] **Vue frontend wciąż działa** (Phase 2.0 nic nie psuje)

```bash
cd /home/dv6/GitHub/storyshelf
make dev-up
# Otworz http://localhost:5173 — Vue ladnie sie laduje, login dziala
make dev-down
```

- [ ] **Lint: 0 błędów**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
uv run ruff check .
```

- [ ] **Prod settings bootuje przy SameSite=None + Secure=True; faila przy SameSite=None + Secure=False**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
# OK
DJANGO_ENV=prod DJANGO_SECRET_KEY=t ALLOWED_HOSTS=e.com CSRF_TRUSTED_ORIGINS=https://e.com \
    JWT_COOKIE_SAMESITE=None JWT_COOKIE_SECURE=true \
    uv run python manage.py check
# Fail fast (oczekiwane ValueError)
DJANGO_ENV=prod DJANGO_SECRET_KEY=t ALLOWED_HOSTS=e.com CSRF_TRUSTED_ORIGINS=https://e.com \
    JWT_COOKIE_SAMESITE=None JWT_COOKIE_SECURE=false \
    uv run python manage.py check || echo "OK: rejected None+insecure"
```
