# Phase 2.1 — Books API Extensions — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dopasować kontrakt Books API do kształtu wymaganego przez SvelteKit: paginacja `{data,page,per_page,total}`, slug na Book, filtry `genre`/`sort`/`q`, endpoint `contains-character`, reviews scoped pod `/books/:id/reviews/`.

**Architecture:** Pięć niezależnych obszarów implementowanych od fundamentów (paginator) ku szczegółom (views, URLs). Każdy task produkuje działający, testowalny kawałek — możliwe wdrożenie incrementalne. Vue compat zachowany przez `?paginate=false` na BookListView i zachowany `POST /api/reviews/` legacy.

**Tech Stack:** Django 6, DRF 3.17, `django-utils-text.slugify`, pytest-django, `uv run python manage.py test`, `DJANGO_ENV=dev`.

---

## Pliki do zmiany

| Plik | Akcja |
|---|---|
| `backend-django/config/pagination.py` | NOWY — `StandardPagination` |
| `backend-django/config/settings/base.py:96-97` | zmiana `DEFAULT_PAGINATION_CLASS` i `PAGE_SIZE` |
| `backend-django/books/models.py` | dodać `slug` + `save()` override + `_generate_unique_slug()` |
| `backend-django/books/migrations/0004_book_slug.py` | NOWA — `AddField` + `RunPython` backfill + `AlterField` |
| `backend-django/books/lookups.py` | NOWY — `resolve_book(id_or_slug) -> Book` |
| `backend-django/books/serializers.py:43-62` | dodać `slug` do `BookListSerializer` i `BookDetailSerializer` |
| `backend-django/books/views.py` | refactor `BookListView` (filters, pagination), nowy `BookContainsCharacterView`, refactor `BookRetrieveView.get_object()` |
| `backend-django/books/urls.py` | dodać `contains-character/`, `<str:id_or_slug>/`, `<str:id_or_slug>/reviews/`; zachować `<int:pk>/` |
| `backend-django/reviews/views.py` | nowy `BookReviewListCreateView` |
| `backend-django/reviews/serializers.py` | nowy `ScopedReviewCreateSerializer` (bez `bookId`) |
| `backend-django/books/tests/test_pagination.py` | NOWY |
| `backend-django/books/tests/test_slug_routing.py` | NOWY |
| `backend-django/books/tests/test_books_filters.py` | NOWY |
| `backend-django/books/tests/test_contains_character.py` | NOWY |
| `backend-django/reviews/tests/test_book_scoped.py` | NOWY |
| `docs/api/openapi.yml` | regeneracja po zakończeniu wszystkich tasków |

---

## Out of scope

- Endpoint `/books/:id/characters` i `/books/:id/relations` — Phase 2.2 (AI extraction)
- PATCH/POST na Book przez API — books są tylko-admin (Django Admin upload)
- Multi-value genre filter (`?genre=Fantasy&genre=Sci-Fi`) — frontend ma single select
- Cross-book character search — ADR-003, encje per-book, cross-book = Phase 3
- Full-text search PostgreSQL (`SearchVector`) — `icontains` wystarczy do <5k książek
- Paginacja na shelf/library/authors/genres endpoints — nie w zakresie Phase 2.1
- Sort po `published_year`, `page_count` — nie ma w wireframe

---

## Task 1: `StandardPagination` — custom paginator + settings

**Files:**
- Create: `backend-django/config/pagination.py`
- Modify: `backend-django/config/settings/base.py:96-97`
- Create: `backend-django/books/tests/test_pagination.py`

- [ ] **Krok 1: Napisz failing test**

Utwórz `backend-django/books/tests/test_pagination.py`:

```python
from django.test import TestCase
from rest_framework.test import APIRequestFactory

from config.pagination import StandardPagination


class StandardPaginationTest(TestCase):
    def _make_paginator(self, page=1, per_page=5):
        factory = APIRequestFactory()
        request = factory.get("/", {"page": page, "per_page": per_page})
        paginator = StandardPagination()
        paginator.request = request
        return paginator

    def test_paginated_response_shape(self):
        """Response ma klucze data/page/per_page/total."""
        from django.test import RequestFactory
        from rest_framework.request import Request

        factory = RequestFactory()
        raw_request = factory.get("/", {"page": 1, "per_page": 3})
        request = Request(raw_request)

        paginator = StandardPagination()
        items = list(range(10))
        result = paginator.paginate_queryset(items, request)
        response = paginator.get_paginated_response(result)

        self.assertEqual(response.data["page"], 1)
        self.assertEqual(response.data["per_page"], 3)
        self.assertEqual(response.data["total"], 10)
        self.assertIn("data", response.data)
        self.assertEqual(len(response.data["data"]), 3)

    def test_default_page_size_is_20(self):
        paginator = StandardPagination()
        self.assertEqual(paginator.page_size, 20)

    def test_max_page_size_is_100(self):
        paginator = StandardPagination()
        self.assertEqual(paginator.max_page_size, 100)
```

- [ ] **Krok 2: Uruchom test — powinien failować**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test books.tests.test_pagination -v 2
```

Oczekiwane: `ImportError: cannot import name 'StandardPagination' from 'config.pagination'` (plik nie istnieje).

- [ ] **Krok 3: Utwórz `config/pagination.py`**

```python
from rest_framework.pagination import PageNumberPagination
from rest_framework.response import Response


class StandardPagination(PageNumberPagination):
    page_size = 20
    page_size_query_param = "per_page"
    max_page_size = 100

    def get_paginated_response(self, data):
        return Response({
            "data": data,
            "page": self.page.number,
            "per_page": self.get_page_size(self.request),
            "total": self.page.paginator.count,
        })

    def get_paginated_response_schema(self, schema):
        return {
            "type": "object",
            "required": ["data", "page", "per_page", "total"],
            "properties": {
                "data": schema,
                "page": {"type": "integer", "example": 1},
                "per_page": {"type": "integer", "example": 20},
                "total": {"type": "integer", "example": 100},
            },
        }
```

- [ ] **Krok 4: Zaktualizuj `config/settings/base.py:96-97`**

Zamień:
```python
    "DEFAULT_PAGINATION_CLASS": "rest_framework.pagination.PageNumberPagination",
    "PAGE_SIZE": 50,
```
Na:
```python
    "DEFAULT_PAGINATION_CLASS": "config.pagination.StandardPagination",
    "PAGE_SIZE": 20,
```

- [ ] **Krok 5: Uruchom test — green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test books.tests.test_pagination -v 2
```

Oczekiwane: 3 PASS.

- [ ] **Krok 6: Pełen test suite — sprawdź czy zmiana paginatora niczego nie zepsuła**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS. Jeśli `books` testy failują na kształcie odpowiedzi — to oczekiwane (BookListView ma jeszcze `pagination_class = None`). Jeśli inne testy failują — zbadaj przyczynę.

- [ ] **Krok 7: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/config/pagination.py backend-django/config/settings/base.py backend-django/books/tests/test_pagination.py
git commit -m "feat(config): StandardPagination with data/page/per_page/total shape"
```

---

## Task 2: `Book.slug` — model + migracja + `resolve_book` lookup

**Files:**
- Modify: `backend-django/books/models.py`
- Create: `backend-django/books/migrations/0004_book_slug.py` (Django generuje automatycznie)
- Create: `backend-django/books/lookups.py`
- Create: `backend-django/books/tests/test_slug_routing.py`

- [ ] **Krok 1: Napisz failing test**

Utwórz `backend-django/books/tests/test_slug_routing.py`:

```python
from django.test import TestCase

from books.models import Book
from books.lookups import resolve_book


class BookSlugGenerationTest(TestCase):
    def test_slug_generated_on_save(self):
        book = Book.objects.create(title="The Great Gatsby")
        self.assertEqual(book.slug, "the-great-gatsby")

    def test_slug_collision_adds_suffix(self):
        Book.objects.create(title="Dune")
        book2 = Book.objects.create(title="Dune")
        self.assertNotEqual(book2.slug, "dune")
        self.assertTrue(book2.slug.startswith("dune-"))

    def test_slug_not_changed_on_title_update(self):
        book = Book.objects.create(title="Original Title")
        original_slug = book.slug
        book.title = "Changed Title"
        book.save()
        book.refresh_from_db()
        self.assertEqual(book.slug, original_slug)


class ResolveBookTest(TestCase):
    def setUp(self):
        self.book = Book.objects.create(title="Hamlet")

    def test_resolve_by_integer_id(self):
        result = resolve_book(str(self.book.pk))
        self.assertEqual(result.pk, self.book.pk)

    def test_resolve_by_slug(self):
        result = resolve_book(self.book.slug)
        self.assertEqual(result.pk, self.book.pk)

    def test_resolve_nonexistent_raises_404(self):
        from django.http import Http404
        with self.assertRaises(Http404):
            resolve_book("nonexistent-slug-xyz")
```

- [ ] **Krok 2: Uruchom test — powinien failować**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test books.tests.test_slug_routing -v 2
```

Oczekiwane: `ImportError` (brak `slug` na modelu i brak `books/lookups.py`).

- [ ] **Krok 3: Dodaj `slug` do `books/models.py`**

Dodaj import na górze pliku (po `from django.db import models`):

```python
from django.utils.text import slugify
```

Dodaj pole `slug` do klasy `Book` po linii 22 (`updated_at`):

```python
    slug = models.SlugField(max_length=255, unique=True, blank=True, default="")
```

Dodaj metodę `save()` do klasy `Book` po `__str__`:

```python
    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = _generate_unique_slug(self.title)
        super().save(*args, **kwargs)
```

Dodaj helper function PRZED klasą `Book` (lub na końcu modułu — przed `BookAuthor`):

```python
def _generate_unique_slug(title: str) -> str:
    base = slugify(title)[:200] or "book"
    slug = base
    counter = 1
    while Book.objects.filter(slug=slug).exists():
        slug = f"{base}-{counter}"
        counter += 1
    return slug
```

**Uwaga:** `_generate_unique_slug` musi być zdefiniowana PRZED `Book.save()`, ale odwołuje się do `Book.objects` — w Pythonie to OK, bo funkcja jest wywołana dopiero w runtime, nie przy definicji klasy.

- [ ] **Krok 4: Utwórz migrację (Django generuje)**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py makemigrations books --name book_slug
```

Oczekiwane: tworzy `books/migrations/0004_book_slug.py` z `AddField(slug, blank=True, default="")`.

- [ ] **Krok 5: Edytuj migrację — dodaj RunPython backfill + AlterField**

Odczytaj wygenerowany `books/migrations/0004_book_slug.py`. Zastąp jego zawartość:

```python
from django.db import migrations, models
from django.utils.text import slugify


def generate_slugs(apps, schema_editor):
    Book = apps.get_model("books", "Book")
    existing_slugs = set()
    for book in Book.objects.all().order_by("id"):
        base = slugify(book.title)[:200] or "book"
        slug = base
        counter = 1
        while slug in existing_slugs or Book.objects.filter(slug=slug).exists():
            slug = f"{base}-{counter}"
            counter += 1
        book.slug = slug
        book.save(update_fields=["slug"])
        existing_slugs.add(slug)


class Migration(migrations.Migration):
    dependencies = [
        ("books", "0003_book_ratings_count"),  # dostosuj do faktycznej ostatniej migracji
    ]

    operations = [
        migrations.AddField(
            model_name="book",
            name="slug",
            field=models.SlugField(blank=True, default="", max_length=255),
        ),
        migrations.RunPython(generate_slugs, reverse_code=migrations.RunPython.noop),
        migrations.AlterField(
            model_name="book",
            name="slug",
            field=models.SlugField(blank=True, max_length=255, unique=True),
        ),
    ]
```

**Sprawdź nazwę ostatniej migracji books** przed edycją:

```bash
ls /home/dv6/GitHub/storyshelf/backend-django/books/migrations/
```

Dostosuj `dependencies` do faktycznej nazwy ostatniej migracji w folderze.

- [ ] **Krok 6: Uruchom migrację**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py migrate
```

Oczekiwane: migracja przechodzi bez błędów. Jeśli baza zawiera dane (seed), istniejące książki dostają slugi.

- [ ] **Krok 7: Utwórz `books/lookups.py`**

```python
from django.http import Http404
from django.shortcuts import get_object_or_404

from .models import Book


def resolve_book(id_or_slug: str) -> Book:
    """Resolve a Book by integer PK (if numeric) or by slug.

    Raises Http404 if not found.
    """
    if id_or_slug.isdigit():
        return get_object_or_404(Book, pk=int(id_or_slug))
    return get_object_or_404(Book, slug=id_or_slug)
```

- [ ] **Krok 8: Uruchom testy slug — green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test books.tests.test_slug_routing -v 2
```

Oczekiwane: 6 PASS.

- [ ] **Krok 9: Pełen test suite**

```bash
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS.

- [ ] **Krok 10: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/books/models.py backend-django/books/migrations/ backend-django/books/lookups.py backend-django/books/tests/test_slug_routing.py
git commit -m "feat(books): add slug field with auto-generation + resolve_book lookup"
```

---

## Task 3: `BookListSerializer` + `BookDetailSerializer` — dodanie `slug`

**Files:**
- Modify: `backend-django/books/serializers.py:43-85`

- [ ] **Krok 1: Dodaj `slug` do `BookListSerializer` (`books/serializers.py:48-62`)**

W `Meta.fields` w `BookListSerializer` dodaj `"slug"` jako pierwszy element (po `"id"`):

```python
class BookListSerializer(BookSerializerMixin, serializers.ModelSerializer):
    author = serializers.SerializerMethodField()
    genres = serializers.SerializerMethodField()
    tags = serializers.SerializerMethodField()

    class Meta:
        model = Book
        fields = (
            "id",
            "slug",
            "title",
            "author",
            "year",
            "isbn",
            "description",
            "page_count",
            "genres",
            "tags",
            "avg_rating",
            "ratings_count",
        )
```

- [ ] **Krok 2: Dodaj `slug` do `BookDetailSerializer` (`books/serializers.py:65-85`)**

W `Meta.fields` w `BookDetailSerializer` dodaj `"slug"` po `"id"`:

```python
    class Meta:
        model = Book
        fields = (
            "id",
            "slug",
            "title",
            "author",
            "year",
            "isbn",
            "description",
            "page_count",
            "genres",
            "tags",
            "avg_rating",
            "ratingsCount",
        )
```

- [ ] **Krok 3: Uruchom testy books — sprawdź że serializery działają**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test books -v 2
```

Oczekiwane: PASS. Jeśli był istniejący test sprawdzający kształt serializera bez `slug` — zaktualizuj go aby zawierał `slug`.

- [ ] **Krok 4: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/books/serializers.py
git commit -m "feat(books): add slug to BookListSerializer and BookDetailSerializer"
```

---

## Task 4: `BookListView` — paginacja + filtry + idOrSlug routing + `BookContainsCharacterView`

**Files:**
- Modify: `backend-django/books/views.py`
- Modify: `backend-django/books/urls.py`
- Create: `backend-django/books/tests/test_books_filters.py`
- Create: `backend-django/books/tests/test_contains_character.py`

- [ ] **Krok 1: Napisz failing testy dla filtrów**

Utwórz `backend-django/books/tests/test_books_filters.py`:

```python
from django.test import TestCase
from rest_framework.test import APIClient

from books.models import Book
from library.models import Genre


class BookFiltersTest(TestCase):
    def setUp(self):
        self.client = APIClient()
        fantasy = Genre.objects.create(name="Fantasy")
        scifi = Genre.objects.create(name="Sci-Fi")

        self.book_fantasy = Book.objects.create(title="A Fantasy Book", avg_rating=4.5, ratings_count=100)
        self.book_fantasy.genres.add(fantasy)

        self.book_scifi = Book.objects.create(title="A Sci-Fi Book", avg_rating=3.0, ratings_count=200)
        self.book_scifi.genres.add(scifi)

        self.book_both = Book.objects.create(title="Sci-Fi Fantasy", avg_rating=5.0, ratings_count=50)
        self.book_both.genres.add(fantasy, scifi)

    def test_filter_by_genre(self):
        response = self.client.get("/api/books/?genre=Fantasy")
        self.assertEqual(response.status_code, 200)
        ids = [b["id"] for b in response.data["data"]]
        self.assertIn(self.book_fantasy.pk, ids)
        self.assertIn(self.book_both.pk, ids)
        self.assertNotIn(self.book_scifi.pk, ids)

    def test_filter_genre_case_insensitive(self):
        response = self.client.get("/api/books/?genre=fantasy")
        self.assertEqual(response.status_code, 200)
        ids = [b["id"] for b in response.data["data"]]
        self.assertIn(self.book_fantasy.pk, ids)

    def test_sort_by_rating(self):
        response = self.client.get("/api/books/?sort=rating")
        self.assertEqual(response.status_code, 200)
        ratings = [b["avg_rating"] for b in response.data["data"]]
        self.assertEqual(ratings, sorted(ratings, reverse=True))

    def test_sort_by_popular(self):
        response = self.client.get("/api/books/?sort=popular")
        self.assertEqual(response.status_code, 200)
        counts = [b["ratings_count"] for b in response.data["data"]]
        self.assertEqual(counts, sorted(counts, reverse=True))

    def test_sort_by_recent(self):
        response = self.client.get("/api/books/?sort=recent")
        self.assertEqual(response.status_code, 200)
        # Only checks it returns 200 and paginated shape (ordering by created_at desc)
        self.assertIn("data", response.data)

    def test_invalid_sort_falls_back_to_title(self):
        response = self.client.get("/api/books/?sort=garbage")
        self.assertEqual(response.status_code, 200)
        titles = [b["title"] for b in response.data["data"]]
        self.assertEqual(titles, sorted(titles))

    def test_combined_filters(self):
        response = self.client.get("/api/books/?genre=Fantasy&sort=rating&q=Fantasy")
        self.assertEqual(response.status_code, 200)
        self.assertIn("data", response.data)

    def test_paginated_shape(self):
        response = self.client.get("/api/books/")
        self.assertEqual(response.status_code, 200)
        for key in ("data", "page", "per_page", "total"):
            self.assertIn(key, response.data)

    def test_paginate_false_returns_flat_list(self):
        """Backward compat dla Vue — ?paginate=false zwraca array."""
        response = self.client.get("/api/books/?paginate=false")
        self.assertEqual(response.status_code, 200)
        self.assertIsInstance(response.data, list)
```

- [ ] **Krok 2: Napisz failing testy dla `contains-character`**

Utwórz `backend-django/books/tests/test_contains_character.py`:

```python
from django.test import TestCase
from rest_framework.test import APIClient

from analysis.models import BookCharacter
from books.models import Book


class ContainsCharacterTest(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.book1 = Book.objects.create(title="Harry Potter")
        self.book2 = Book.objects.create(title="Another Book")
        BookCharacter.objects.create(book=self.book1, name="Hermione Granger", mention_count=10)
        BookCharacter.objects.create(book=self.book1, name="Harry Potter", mention_count=20)
        BookCharacter.objects.create(book=self.book2, name="John Smith", mention_count=5)

    def test_finds_book_by_character_name(self):
        response = self.client.get("/api/books/contains-character/?name=Hermione")
        self.assertEqual(response.status_code, 200)
        ids = [b["id"] for b in response.data["data"]]
        self.assertIn(self.book1.pk, ids)
        self.assertNotIn(self.book2.pk, ids)

    def test_case_insensitive(self):
        response = self.client.get("/api/books/contains-character/?name=hermione")
        self.assertEqual(response.status_code, 200)
        ids = [b["id"] for b in response.data["data"]]
        self.assertIn(self.book1.pk, ids)

    def test_substring_match(self):
        response = self.client.get("/api/books/contains-character/?name=Herm")
        self.assertEqual(response.status_code, 200)
        ids = [b["id"] for b in response.data["data"]]
        self.assertIn(self.book1.pk, ids)

    def test_no_duplicates_when_multiple_characters_match(self):
        # book1 has two characters matching "Harry"
        response = self.client.get("/api/books/contains-character/?name=Harry")
        self.assertEqual(response.status_code, 200)
        ids = [b["id"] for b in response.data["data"]]
        self.assertEqual(len(ids), len(set(ids)), "Duplicate books in response")

    def test_empty_name_returns_empty(self):
        response = self.client.get("/api/books/contains-character/")
        self.assertEqual(response.status_code, 200)
        self.assertEqual(response.data["data"], [])
        self.assertEqual(response.data["total"], 0)

    def test_paginated_shape(self):
        response = self.client.get("/api/books/contains-character/?name=a")
        self.assertEqual(response.status_code, 200)
        for key in ("data", "page", "per_page", "total"):
            self.assertIn(key, response.data)
```

- [ ] **Krok 3: Uruchom testy — powinny failować (views/URL nie istnieją)**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test books.tests.test_books_filters books.tests.test_contains_character -v 2
```

Oczekiwane: FAIL (połączenie różnych błędów — brak paginacji, brak view).

- [ ] **Krok 4: Przebuduj `books/views.py`**

Zastąp całą zawartość `backend-django/books/views.py`:

```python
from django.db.models import Prefetch, Q
from rest_framework import generics, permissions
from rest_framework.response import Response

from shelf.models import ShelfEntry

from .lookups import resolve_book
from .models import Book
from .serializers import BookDetailSerializer, BookListSerializer

SORT_MAP = {
    "rating": ["-avg_rating", "-ratings_count"],
    "recent": ["-created_at", "-id"],
    "popular": ["-ratings_count", "-avg_rating"],
}
DEFAULT_ORDER = ["title"]


class BookListView(generics.ListAPIView):
    serializer_class = BookListSerializer
    permission_classes = [permissions.AllowAny]

    def get_queryset(self):
        qs = Book.objects.select_related("serie").prefetch_related("authors", "tags", "genres")

        q = self.request.query_params.get("q", "").strip()
        if q:
            qs = qs.filter(
                Q(title__icontains=q)
                | Q(bookauthor__author__name__icontains=q)
                | Q(genres__name__icontains=q)
            ).distinct()

        genre = self.request.query_params.get("genre", "").strip()
        if genre:
            qs = qs.filter(genres__name__iexact=genre).distinct()

        sort = self.request.query_params.get("sort", "").strip()
        qs = qs.order_by(*SORT_MAP.get(sort, DEFAULT_ORDER))

        return qs

    def list(self, request, *args, **kwargs):
        if request.query_params.get("paginate") == "false":
            queryset = self.filter_queryset(self.get_queryset())
            serializer = self.get_serializer(queryset, many=True)
            return Response(serializer.data)
        return super().list(request, *args, **kwargs)


class BookRetrieveView(generics.RetrieveAPIView):
    serializer_class = BookDetailSerializer
    permission_classes = [permissions.AllowAny]

    def get_object(self):
        id_or_slug = self.kwargs["id_or_slug"]
        qs = Book.objects.select_related("serie").prefetch_related(
            "authors",
            "tags",
            "genres",
            "characters",
            "character_relationships__from_character",
            "character_relationships__to_character",
        )
        if self.request.user.is_authenticated:
            qs = qs.prefetch_related(
                Prefetch(
                    "shelf_entries",
                    queryset=ShelfEntry.objects.filter(user=self.request.user),
                    to_attr="current_user_shelf_entries",
                )
            )
        book = resolve_book(id_or_slug)
        # Re-fetch with prefetch from qs
        return qs.get(pk=book.pk)


class BookContainsCharacterView(generics.ListAPIView):
    serializer_class = BookListSerializer
    permission_classes = [permissions.AllowAny]

    def get_queryset(self):
        name = self.request.query_params.get("name", "").strip()
        if not name:
            return Book.objects.none()
        return (
            Book.objects.filter(characters__name__icontains=name)
            .select_related("serie")
            .prefetch_related("authors", "genres", "tags")
            .distinct()
            .order_by("title")
        )
```

- [ ] **Krok 5: Zaktualizuj `books/urls.py`**

Zastąp całą zawartość `backend-django/books/urls.py`:

```python
from django.urls import path

from .views import BookContainsCharacterView, BookListView, BookRetrieveView

urlpatterns = [
    path("", BookListView.as_view()),
    # contains-character MUSI byc przed <str:id_or_slug> — Django dopasowuje po kolei
    path("contains-character/", BookContainsCharacterView.as_view()),
    # Legacy int-PK routing (backward compat dla Vue do Phase 2.6)
    path("<int:pk>/", BookRetrieveView.as_view()),
    # idOrSlug routing (nowy kontrakt Svelte)
    path("<str:id_or_slug>/", BookRetrieveView.as_view()),
    # Scoped reviews — montowany tutaj, view pochodzi z reviews app
    # (dodawany w Task 5)
]
```

**Uwaga:** `BookRetrieveView` obsługuje teraz zarówno `<int:pk>` jak i `<str:id_or_slug>`. Dla `<int:pk>` Django przekaże `pk` w `kwargs`, a `id_or_slug` będzie brakować — `get_object()` odczyta `kwargs["id_or_slug"]` co wywali `KeyError`. Rozwiąż to dodając obsługę obu kluczy w `get_object()`:

Zaktualizuj `BookRetrieveView.get_object()` w `books/views.py`:

```python
    def get_object(self):
        id_or_slug = self.kwargs.get("id_or_slug") or str(self.kwargs.get("pk", ""))
        qs = Book.objects.select_related("serie").prefetch_related(
            "authors",
            "tags",
            "genres",
            "characters",
            "character_relationships__from_character",
            "character_relationships__to_character",
        )
        if self.request.user.is_authenticated:
            qs = qs.prefetch_related(
                Prefetch(
                    "shelf_entries",
                    queryset=ShelfEntry.objects.filter(user=self.request.user),
                    to_attr="current_user_shelf_entries",
                )
            )
        book = resolve_book(id_or_slug)
        return qs.get(pk=book.pk)
```

- [ ] **Krok 6: Uruchom testy filtrów i contains-character**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test books.tests.test_books_filters books.tests.test_contains_character -v 2
```

Oczekiwane: wszystkie PASS. Jeśli `library.models.Genre` nie istnieje pod tą ścieżką importu — sprawdź faktyczny import z `library/models.py`:

```bash
grep -n "class Genre" /home/dv6/GitHub/storyshelf/backend-django/library/models.py
```

Dostosuj import w test file odpowiednio.

- [ ] **Krok 7: Pełen test suite**

```bash
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS, w szczególności istniejące testy `books` (routing `<int:pk>` nadal działa).

- [ ] **Krok 8: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/books/views.py backend-django/books/urls.py backend-django/books/tests/test_books_filters.py backend-django/books/tests/test_contains_character.py
git commit -m "feat(books): genre/sort filters, idOrSlug routing, contains-character endpoint"
```

---

## Task 5: Scoped reviews — `BookReviewListCreateView`

**Files:**
- Modify: `backend-django/reviews/serializers.py`
- Modify: `backend-django/reviews/views.py`
- Modify: `backend-django/books/urls.py`
- Create: `backend-django/reviews/tests/test_book_scoped.py`

- [ ] **Krok 1: Napisz failing testy**

Utwórz `backend-django/reviews/tests/test_book_scoped.py`:

```python
from django.test import TestCase
from rest_framework import status
from rest_framework.test import APIClient

from books.models import Book
from reviews.models import Review
from users.models import User


class BookScopedReviewsTest(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.user = User.objects.create_user(
            email="reviewer@test.com", username="reviewer", password="pass123"
        )
        self.book = Book.objects.create(title="Test Book")
        self.other_book = Book.objects.create(title="Other Book")
        Review.objects.create(book=self.book, user=self.user, rating=4, content="Great")
        Review.objects.create(book=self.other_book, user=self.user, rating=3, content="OK")

    def test_list_reviews_scoped_to_book_by_id(self):
        response = self.client.get(f"/api/books/{self.book.pk}/reviews/")
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.assertIn("data", response.data)
        self.assertEqual(len(response.data["data"]), 1)
        self.assertEqual(response.data["data"][0]["rating"], 4)

    def test_list_reviews_scoped_to_book_by_slug(self):
        response = self.client.get(f"/api/books/{self.book.slug}/reviews/")
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.assertEqual(len(response.data["data"]), 1)

    def test_list_reviews_returns_paginated_shape(self):
        response = self.client.get(f"/api/books/{self.book.pk}/reviews/")
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        for key in ("data", "page", "per_page", "total"):
            self.assertIn(key, response.data)

    def test_create_review_scoped_no_book_id_in_body(self):
        self.client.force_authenticate(user=self.user)
        # New book — user has no review yet
        new_book = Book.objects.create(title="Brand New Book")
        response = self.client.post(
            f"/api/books/{new_book.pk}/reviews/",
            {"rating": 5, "content": "Amazing"},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_201_CREATED)
        self.assertEqual(Review.objects.filter(book=new_book).count(), 1)

    def test_create_review_requires_authentication(self):
        new_book = Book.objects.create(title="Auth Test Book")
        response = self.client.post(
            f"/api/books/{new_book.pk}/reviews/",
            {"rating": 4, "content": "Test"},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_401_UNAUTHORIZED)

    def test_create_review_404_for_nonexistent_book(self):
        self.client.force_authenticate(user=self.user)
        response = self.client.post(
            "/api/books/99999/reviews/",
            {"rating": 3, "content": "Ghost"},
            format="json",
        )
        self.assertEqual(response.status_code, status.HTTP_404_NOT_FOUND)
```

- [ ] **Krok 2: Uruchom test — powinien failować**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test reviews.tests.test_book_scoped -v 2
```

Oczekiwane: FAIL — URL `/api/books/<pk>/reviews/` zwraca 404 (nie ma w URLconf).

- [ ] **Krok 3: Dodaj `ScopedReviewCreateSerializer` do `reviews/serializers.py`**

Dopisz na końcu `backend-django/reviews/serializers.py`:

```python
class ScopedReviewCreateSerializer(serializers.ModelSerializer):
    """Review create serializer for scoped endpoint /books/:id/reviews/.

    Book is injected from URL — no bookId field in body.
    """

    class Meta:
        model = Review
        fields = ("id", "rating", "content")
        read_only_fields = ("id",)

    def validate(self, data):
        user = self.context["request"].user
        book = self.context["book"]
        if self.instance is None and Review.objects.filter(user=user, book=book).exists():
            raise serializers.ValidationError("You have already reviewed this book.")
        return data
```

- [ ] **Krok 4: Dodaj `BookReviewListCreateView` do `reviews/views.py`**

Dopisz na końcu `backend-django/reviews/views.py`:

```python
from books.lookups import resolve_book  # noqa: E402 — na końcu by uniknąć circular import


class BookReviewListCreateView(generics.ListCreateAPIView):
    """Scoped reviews for a single book: GET /books/:id/reviews/, POST /books/:id/reviews/."""

    def get_permissions(self):
        if self.request.method == "POST":
            return [permissions.IsAuthenticated()]
        return [permissions.AllowAny()]

    def get_serializer_class(self):
        if self.request.method == "POST":
            return ReviewCreateSerializer  # imported at top of file — uses ScopedReviewCreateSerializer below
        return ReviewSerializer

    def _get_book(self):
        if not hasattr(self, "_book"):
            self._book = resolve_book(self.kwargs["id_or_slug"])
        return self._book

    def get_serializer_class(self):
        from .serializers import ScopedReviewCreateSerializer
        if self.request.method == "POST":
            return ScopedReviewCreateSerializer
        return ReviewSerializer

    def get_serializer_context(self):
        ctx = super().get_serializer_context()
        ctx["book"] = self._get_book()
        return ctx

    def get_queryset(self):
        book = self._get_book()
        return Review.objects.filter(book=book).select_related("user", "book").order_by("-created_at")

    def perform_create(self, serializer):
        serializer.save(user=self.request.user, book=self._get_book())

    def create(self, request, *args, **kwargs):
        try:
            return super().create(request, *args, **kwargs)
        except IntegrityError:
            return Response(
                {"detail": "You have already reviewed this book."},
                status=http_status.HTTP_400_BAD_REQUEST,
            )
```

**Uwaga:** `get_serializer_class` jest zduplikowany — usuń pierwsze wystąpienie (to z `ReviewCreateSerializer`) i zostaw tylko drugie (z `ScopedReviewCreateSerializer`). Sprawdź też że `IntegrityError` i `http_status` są importowane na górze pliku (są w istniejącym kodzie).

- [ ] **Krok 5: Dodaj URL dla scoped reviews do `books/urls.py`**

Dodaj na końcu `urlpatterns` w `backend-django/books/urls.py`:

```python
from reviews.views import BookReviewListCreateView  # dodaj import na górze pliku

# ... w urlpatterns dodaj:
path("<str:id_or_slug>/reviews/", BookReviewListCreateView.as_view()),
```

Finalny `books/urls.py`:

```python
from django.urls import path

from reviews.views import BookReviewListCreateView

from .views import BookContainsCharacterView, BookListView, BookRetrieveView

urlpatterns = [
    path("", BookListView.as_view()),
    path("contains-character/", BookContainsCharacterView.as_view()),
    path("<int:pk>/", BookRetrieveView.as_view()),
    path("<str:id_or_slug>/", BookRetrieveView.as_view()),
    path("<str:id_or_slug>/reviews/", BookReviewListCreateView.as_view()),
]
```

**Problem z kolejnością:** `<str:id_or_slug>/` i `<str:id_or_slug>/reviews/` — Django **nie** jest greedy przy `<str:>`. URL `/api/books/the-great-gatsby/reviews/` zostanie dopasowany do `<str:id_or_slug>/reviews/` a nie do `<str:id_or_slug>/`. Działa poprawnie — Django testuje wzorce od góry i bierze pierwszy dopasowany.

- [ ] **Krok 6: Uruchom testy scoped reviews — green**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test reviews.tests.test_book_scoped -v 2
```

Oczekiwane: 6 PASS.

- [ ] **Krok 7: Pełen test suite**

```bash
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS. Legacy `POST /api/reviews/` nadal działa (nie dotknęliśmy `reviews/urls.py`).

- [ ] **Krok 8: Lint**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
uv run ruff check .
```

Oczekiwane: 0 błędów.

- [ ] **Krok 9: Commit**

```bash
cd /home/dv6/GitHub/storyshelf
git add backend-django/reviews/serializers.py backend-django/reviews/views.py backend-django/books/urls.py backend-django/reviews/tests/test_book_scoped.py
git commit -m "feat(reviews): scoped reviews endpoint GET/POST /books/:id/reviews/"
```

---

## Task 6: Regeneracja OpenAPI snapshot + akceptacja końcowa

**Files:**
- Modify: `docs/api/openapi.yml` (regeneracja)

- [ ] **Krok 1: Pełen test suite finalny**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test
```

Oczekiwane: wszystkie PASS.

- [ ] **Krok 2: Lint finalny**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
uv run ruff check .
```

Oczekiwane: 0 błędów.

- [ ] **Krok 3: Regeneruj OpenAPI snapshot**

```bash
cd /home/dv6/GitHub/storyshelf
make regenerate-openapi
```

Oczekiwane: `docs/api/openapi.yml` zaktualizowany z nowymi endpointami (`contains-character`, `/{id_or_slug}/reviews/`, `slug` w schemacie Book).

- [ ] **Krok 4: Uruchom snapshot test — musi przejść**

```bash
cd /home/dv6/GitHub/storyshelf/backend-django
DJANGO_ENV=dev uv run python manage.py test config.tests.test_openapi_schema -v 2
```

Oczekiwane: PASS — wygenerowany schemat = zacommittowany snapshot.

- [ ] **Krok 5: Ręczna weryfikacja endpointów**

```bash
cd /home/dv6/GitHub/storyshelf
make dev-up
```

Sprawdź ręcznie (curl lub przeglądarka):

```bash
# Paginated list
curl "http://localhost:8000/api/books/?page=1&per_page=5" | python -m json.tool | head -30

# Vue compat
curl "http://localhost:8000/api/books/?paginate=false" | python -m json.tool | head -5
# Oczekiwane: array, nie obiekt

# Slug routing
SLUG=$(curl -s "http://localhost:8000/api/books/?per_page=1" | python -c "import sys,json; d=json.load(sys.stdin); print(d['data'][0]['slug'])")
curl "http://localhost:8000/api/books/$SLUG/" | python -m json.tool | head -10

# Filtry
curl "http://localhost:8000/api/books/?genre=Fantasy&sort=rating" | python -m json.tool | head -10

# Contains character (wymaga danych z seeda)
curl "http://localhost:8000/api/books/contains-character/?name=Frodo" | python -m json.tool

make dev-down
```

- [ ] **Krok 6: Commit snapshot**

```bash
cd /home/dv6/GitHub/storyshelf
git add docs/api/openapi.yml
git commit -m "chore(api): regenerate OpenAPI snapshot after Phase 2.1 changes"
```

---

## Akceptacja całego planu

- [ ] `DJANGO_ENV=dev uv run python manage.py test` — wszystkie testy PASS
- [ ] `uv run ruff check .` — 0 błędów
- [ ] `GET /api/books/?page=1&per_page=20` → `{data, page, per_page, total}`
- [ ] `GET /api/books/?paginate=false` → płaska lista (array)
- [ ] `GET /api/books/?genre=Fantasy&sort=rating&q=ring` — wszystkie 3 filtry działają
- [ ] `GET /api/books/<int_id>/` i `GET /api/books/<slug>/` — oba zwracają tę samą książkę
- [ ] `GET /api/books/contains-character/?name=<name>` → paginated lista
- [ ] `GET /api/books/<id_or_slug>/reviews/` → paginated reviews
- [ ] `POST /api/books/<id>/reviews/` z body `{rating, content}` (bez `bookId`) — 201
- [ ] Legacy `POST /api/reviews/` z `bookId` w body — nadal działa (Vue compat)
- [ ] `docs/api/openapi.yml` zaktualizowany i zacommittowany
- [ ] Nowe slugi na istniejących książkach po `manage.py migrate`
